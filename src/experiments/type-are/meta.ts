import type { ExperimentMeta } from '../../core/Experiment.ts';

export const meta: ExperimentMeta = {
  slug: 'type-are',
  title: 'type are',
  description:
    'HTML-in-Canvas dithered typography — real CSS-styled text captured via the drawElementImage API, then shattered through halftone, ordered, noise, crosshatch, and scanline dithering shaders.',
  tags: ['glsl', 'dither', 'typography', 'html-in-canvas', 'css'],
  date: '2026-04-08',
  hasArticle: false,
  draft: true,
};
