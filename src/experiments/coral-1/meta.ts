import type { ExperimentMeta } from '../../core/Experiment.ts';

export const meta: ExperimentMeta = {
  slug: 'coral-1',
  title: 'Coral-1 · Reaction Field',
  description:
    'Gray-Scott reaction-diffusion grown on the GPU. Feed/kill maps vary across the canvas, diffusion can be steered directionally, and an uploaded image can seed, mask or densify the growth. Flat, outline, emboss, gradient and halftone renders; SVG/PNG export via the Brand Tool.',
  tags: ['reaction-diffusion', 'gray-scott', 'coral', 'brand', 'pattern', 'webgl2'],
  date: '2026-08-20',
  hasArticle: false,
};
