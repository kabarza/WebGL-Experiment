import type { ExperimentMeta } from '../../core/Experiment.ts';

export const meta: ExperimentMeta = {
  slug: 'type-dither',
  title: 'type dither',
  description:
    'Dithered typography — render custom text through halftone, ordered, noise, crosshatch, and scanline dithering filters with animated distortion.',
  tags: ['glsl', 'dither', 'typography', 'text'],
  date: '2026-04-08',
  hasArticle: false,
};
