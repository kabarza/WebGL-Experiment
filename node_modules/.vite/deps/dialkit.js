"use client";
import {
  require_react_dom
} from "./chunk-77XXEOXN.js";
import {
  AnimatePresence,
  animate,
  motion,
  useMotionValue,
  useTransform
} from "./chunk-MPX6C4WS.js";
import {
  require_jsx_runtime
} from "./chunk-6QO6Q4YR.js";
import {
  require_react
} from "./chunk-NKU67ILE.js";
import {
  __toESM
} from "./chunk-PR4QN5HX.js";

// node_modules/dialkit/dist/index.js
var import_react = __toESM(require_react());
var import_react2 = __toESM(require_react());
var import_react_dom = __toESM(require_react_dom());
var import_react3 = __toESM(require_react());
var import_react5 = __toESM(require_react());
var import_jsx_runtime = __toESM(require_jsx_runtime());
var import_react6 = __toESM(require_react());
var import_jsx_runtime2 = __toESM(require_jsx_runtime());
var import_react8 = __toESM(require_react());
var import_jsx_runtime3 = __toESM(require_jsx_runtime());
var import_react10 = __toESM(require_react());
var import_jsx_runtime4 = __toESM(require_jsx_runtime());
var import_jsx_runtime5 = __toESM(require_jsx_runtime());
var import_jsx_runtime6 = __toESM(require_jsx_runtime());
var import_react11 = __toESM(require_react());
var import_jsx_runtime7 = __toESM(require_jsx_runtime());
var import_jsx_runtime8 = __toESM(require_jsx_runtime());
var import_react12 = __toESM(require_react());
var import_jsx_runtime9 = __toESM(require_jsx_runtime());
var import_jsx_runtime10 = __toESM(require_jsx_runtime());
var import_react13 = __toESM(require_react());
var import_react_dom2 = __toESM(require_react_dom());
var import_jsx_runtime11 = __toESM(require_jsx_runtime());
var import_react15 = __toESM(require_react());
var import_jsx_runtime12 = __toESM(require_jsx_runtime());
var import_react16 = __toESM(require_react());
var import_react_dom3 = __toESM(require_react_dom());
var import_jsx_runtime13 = __toESM(require_jsx_runtime());
var import_react18 = __toESM(require_react());
var import_jsx_runtime14 = __toESM(require_jsx_runtime());
var import_react20 = __toESM(require_react());
var import_jsx_runtime15 = __toESM(require_jsx_runtime());
var import_jsx_runtime16 = __toESM(require_jsx_runtime());
var import_jsx_runtime17 = __toESM(require_jsx_runtime());
var import_jsx_runtime18 = __toESM(require_jsx_runtime());
var import_react21 = __toESM(require_react());
var import_react_dom4 = __toESM(require_react_dom());
var import_jsx_runtime19 = __toESM(require_jsx_runtime());
var EMPTY_VALUES = Object.freeze({});
var DialStoreClass = class {
  constructor() {
    this.panels = /* @__PURE__ */ new Map();
    this.listeners = /* @__PURE__ */ new Map();
    this.globalListeners = /* @__PURE__ */ new Set();
    this.snapshots = /* @__PURE__ */ new Map();
    this.actionListeners = /* @__PURE__ */ new Map();
    this.presets = /* @__PURE__ */ new Map();
    this.activePreset = /* @__PURE__ */ new Map();
    this.baseValues = /* @__PURE__ */ new Map();
  }
  registerPanel(id, name, config, shortcuts) {
    const controls = this.parseConfig(config, "", shortcuts);
    const values = this.flattenValues(config, "");
    this.initTransitionModes(config, "", values);
    this.panels.set(id, { id, name, controls, values, shortcuts: shortcuts ?? {} });
    this.snapshots.set(id, { ...values });
    this.baseValues.set(id, { ...values });
    this.notifyGlobal();
  }
  updatePanel(id, name, config, shortcuts) {
    const existing = this.panels.get(id);
    if (!existing) {
      this.registerPanel(id, name, config, shortcuts);
      return;
    }
    const controls = this.parseConfig(config, "", shortcuts);
    const controlsByPath = this.mapControlsByPath(controls);
    const defaultValues = this.flattenValues(config, "");
    const nextValues = {};
    for (const [path, defaultValue] of Object.entries(defaultValues)) {
      nextValues[path] = this.normalizePreservedValue(
        existing.values[path],
        defaultValue,
        controlsByPath.get(path)
      );
    }
    this.initTransitionModes(config, "", nextValues);
    for (const [path, mode] of Object.entries(existing.values)) {
      if (!path.endsWith(".__mode")) {
        continue;
      }
      const transitionPath = path.slice(0, -"__mode".length - 1);
      const transitionControl = controlsByPath.get(transitionPath);
      if ((transitionControl == null ? void 0 : transitionControl.type) === "transition") {
        nextValues[path] = mode;
      }
    }
    const nextPanel = { id, name, controls, values: nextValues, shortcuts: shortcuts ?? existing.shortcuts };
    this.panels.set(id, nextPanel);
    this.snapshots.set(id, { ...nextValues });
    const previousBaseValues = this.baseValues.get(id) ?? {};
    const nextBaseValues = {};
    for (const [path, defaultValue] of Object.entries(defaultValues)) {
      nextBaseValues[path] = this.normalizePreservedValue(
        previousBaseValues[path],
        defaultValue,
        controlsByPath.get(path)
      );
    }
    for (const [path, value] of Object.entries(nextValues)) {
      if (path.endsWith(".__mode")) {
        nextBaseValues[path] = value;
      }
    }
    this.baseValues.set(id, nextBaseValues);
    this.notify(id);
    this.notifyGlobal();
  }
  unregisterPanel(id) {
    this.panels.delete(id);
    this.listeners.delete(id);
    this.snapshots.delete(id);
    this.actionListeners.delete(id);
    this.baseValues.delete(id);
    this.notifyGlobal();
  }
  updateValue(panelId, path, value) {
    const panel = this.panels.get(panelId);
    if (!panel) return;
    panel.values[path] = value;
    const activeId = this.activePreset.get(panelId);
    if (activeId) {
      const presets = this.presets.get(panelId) ?? [];
      const preset = presets.find((p) => p.id === activeId);
      if (preset) preset.values[path] = value;
    } else {
      const base = this.baseValues.get(panelId);
      if (base) base[path] = value;
    }
    this.snapshots.set(panelId, { ...panel.values });
    this.notify(panelId);
  }
  updateSpringMode(panelId, path, mode) {
    this.updateTransitionMode(panelId, path, mode);
  }
  getSpringMode(panelId, path) {
    const mode = this.getTransitionMode(panelId, path);
    if (mode === "easing") return "simple";
    return mode;
  }
  updateTransitionMode(panelId, path, mode) {
    const panel = this.panels.get(panelId);
    if (!panel) return;
    panel.values[`${path}.__mode`] = mode;
    this.snapshots.set(panelId, { ...panel.values });
    this.notify(panelId);
  }
  getTransitionMode(panelId, path) {
    const panel = this.panels.get(panelId);
    if (!panel) return "simple";
    return panel.values[`${path}.__mode`] || "simple";
  }
  getValue(panelId, path) {
    const panel = this.panels.get(panelId);
    return panel == null ? void 0 : panel.values[path];
  }
  getValues(panelId) {
    return this.snapshots.get(panelId) ?? EMPTY_VALUES;
  }
  getPanels() {
    return Array.from(this.panels.values());
  }
  getPanel(id) {
    return this.panels.get(id);
  }
  subscribe(panelId, listener) {
    if (!this.listeners.has(panelId)) {
      this.listeners.set(panelId, /* @__PURE__ */ new Set());
    }
    this.listeners.get(panelId).add(listener);
    return () => {
      var _a2;
      (_a2 = this.listeners.get(panelId)) == null ? void 0 : _a2.delete(listener);
    };
  }
  subscribeGlobal(listener) {
    this.globalListeners.add(listener);
    return () => this.globalListeners.delete(listener);
  }
  subscribeActions(panelId, listener) {
    if (!this.actionListeners.has(panelId)) {
      this.actionListeners.set(panelId, /* @__PURE__ */ new Set());
    }
    this.actionListeners.get(panelId).add(listener);
    return () => {
      var _a2;
      (_a2 = this.actionListeners.get(panelId)) == null ? void 0 : _a2.delete(listener);
    };
  }
  triggerAction(panelId, path) {
    var _a2;
    (_a2 = this.actionListeners.get(panelId)) == null ? void 0 : _a2.forEach((fn) => fn(path));
  }
  savePreset(panelId, name) {
    const panel = this.panels.get(panelId);
    if (!panel) throw new Error(`Panel ${panelId} not found`);
    const id = `preset-${Date.now()}-${Math.random().toString(36).slice(2, 9)}`;
    const preset = {
      id,
      name,
      values: { ...panel.values }
    };
    const existing = this.presets.get(panelId) ?? [];
    this.presets.set(panelId, [...existing, preset]);
    this.activePreset.set(panelId, id);
    this.snapshots.set(panelId, { ...panel.values });
    this.notify(panelId);
    return id;
  }
  loadPreset(panelId, presetId) {
    const panel = this.panels.get(panelId);
    if (!panel) return;
    const presets = this.presets.get(panelId) ?? [];
    const preset = presets.find((p) => p.id === presetId);
    if (!preset) return;
    panel.values = { ...preset.values };
    this.snapshots.set(panelId, { ...panel.values });
    this.activePreset.set(panelId, presetId);
    this.notify(panelId);
  }
  deletePreset(panelId, presetId) {
    const presets = this.presets.get(panelId) ?? [];
    this.presets.set(panelId, presets.filter((p) => p.id !== presetId));
    if (this.activePreset.get(panelId) === presetId) {
      this.activePreset.set(panelId, null);
    }
    const panel = this.panels.get(panelId);
    if (panel) {
      this.snapshots.set(panelId, { ...panel.values });
    }
    this.notify(panelId);
  }
  getPresets(panelId) {
    return this.presets.get(panelId) ?? [];
  }
  getActivePresetId(panelId) {
    return this.activePreset.get(panelId) ?? null;
  }
  clearActivePreset(panelId) {
    const panel = this.panels.get(panelId);
    const base = this.baseValues.get(panelId);
    if (panel && base) {
      panel.values = { ...base };
      this.snapshots.set(panelId, { ...panel.values });
    }
    this.activePreset.set(panelId, null);
    this.notify(panelId);
  }
  /**
   * Seed named presets from experiment config. Each preset's values
   * are merged with the current base panel values so loadPreset gets
   * a complete snapshot. Only seeds if no presets exist yet.
   */
  seedPresets(panelId, presets) {
    const panel = this.panels.get(panelId);
    if (!panel) return;
    const existing = this.presets.get(panelId) ?? [];
    if (existing.length > 0) return;
    const seeded = presets.map(({ name, values }) => ({
      id: `preset-${Date.now()}-${Math.random().toString(36).slice(2, 9)}`,
      name,
      values: { ...panel.values, ...values }
    }));
    this.presets.set(panelId, seeded);
    this.snapshots.set(panelId, { ...panel.values });
    this.notify(panelId);
  }
  resolveShortcutTarget(key, modifier) {
    for (const panel of this.panels.values()) {
      for (const [path, shortcut] of Object.entries(panel.shortcuts)) {
        if (!shortcut.key) continue;
        if (shortcut.key.toLowerCase() !== key.toLowerCase()) continue;
        const scMod = shortcut.modifier ?? void 0;
        if (scMod !== modifier) continue;
        const control = this.findControlByPath(panel.controls, path);
        if (control) {
          return { panelId: panel.id, path, control };
        }
      }
    }
    return null;
  }
  resolveScrollOnlyTargets() {
    const results = [];
    for (const panel of this.panels.values()) {
      for (const [path, shortcut] of Object.entries(panel.shortcuts)) {
        if ((shortcut.interaction ?? "scroll") !== "scroll-only") continue;
        const control = this.findControlByPath(panel.controls, path);
        if (control) {
          results.push({ panelId: panel.id, path, control, shortcut });
        }
      }
    }
    return results;
  }
  findControlByPath(controls, path) {
    for (const control of controls) {
      if (control.path === path) return control;
      if (control.type === "folder" && control.children) {
        const found = this.findControlByPath(control.children, path);
        if (found) return found;
      }
    }
    return null;
  }
  notify(panelId) {
    var _a2;
    (_a2 = this.listeners.get(panelId)) == null ? void 0 : _a2.forEach((fn) => fn());
  }
  notifyGlobal() {
    this.globalListeners.forEach((fn) => fn());
  }
  initTransitionModes(config, prefix, values) {
    for (const [key, value] of Object.entries(config)) {
      if (key === "_collapsed") continue;
      const path = prefix ? `${prefix}.${key}` : key;
      if (this.isEasingConfig(value)) {
        values[`${path}.__mode`] = "easing";
      } else if (this.isSpringConfig(value)) {
        const hasPhysics = value.stiffness !== void 0 || value.damping !== void 0 || value.mass !== void 0;
        const hasTime = value.visualDuration !== void 0 || value.bounce !== void 0;
        values[`${path}.__mode`] = hasPhysics && !hasTime ? "advanced" : "simple";
      } else if (typeof value === "object" && value !== null && !Array.isArray(value) && !this.isActionConfig(value) && !this.isSelectConfig(value) && !this.isColorConfig(value) && !this.isTextConfig(value)) {
        this.initTransitionModes(value, path, values);
      }
    }
  }
  parseConfig(config, prefix, shortcuts) {
    const controls = [];
    for (const [key, value] of Object.entries(config)) {
      if (key === "_collapsed") continue;
      const path = prefix ? `${prefix}.${key}` : key;
      const label = this.formatLabel(key);
      const shortcut = shortcuts == null ? void 0 : shortcuts[path];
      if (Array.isArray(value) && value.length <= 4 && typeof value[0] === "number") {
        controls.push({
          type: "slider",
          path,
          label,
          min: value[1],
          max: value[2],
          step: value[3] ?? this.inferStep(value[1], value[2]),
          shortcut
        });
      } else if (typeof value === "number") {
        const { min, max, step } = this.inferRange(value);
        controls.push({ type: "slider", path, label, min, max, step, shortcut });
      } else if (typeof value === "boolean") {
        controls.push({ type: "toggle", path, label, shortcut });
      } else if (this.isSpringConfig(value) || this.isEasingConfig(value)) {
        controls.push({ type: "transition", path, label });
      } else if (this.isActionConfig(value)) {
        controls.push({ type: "action", path, label: value.label || label });
      } else if (this.isSelectConfig(value)) {
        controls.push({ type: "select", path, label, options: value.options });
      } else if (this.isColorConfig(value)) {
        controls.push({ type: "color", path, label });
      } else if (this.isTextConfig(value)) {
        controls.push({ type: "text", path, label, placeholder: value.placeholder });
      } else if (this.isUBConfig(value)) {
        controls.push({ type: value.type, path, label, step: value.step ?? 1, options: value.options });
      } else if (typeof value === "string") {
        if (this.isHexColor(value)) {
          controls.push({ type: "color", path, label });
        } else {
          controls.push({ type: "text", path, label });
        }
      } else if (typeof value === "object" && value !== null) {
        const folderConfig = value;
        const defaultOpen = "_collapsed" in folderConfig ? !folderConfig._collapsed : true;
        controls.push({
          type: "folder",
          path,
          label,
          defaultOpen,
          children: this.parseConfig(folderConfig, path, shortcuts)
        });
      }
    }
    return controls;
  }
  flattenValues(config, prefix) {
    const values = {};
    for (const [key, value] of Object.entries(config)) {
      if (key === "_collapsed") continue;
      const path = prefix ? `${prefix}.${key}` : key;
      if (Array.isArray(value) && value.length <= 4 && typeof value[0] === "number") {
        values[path] = value[0];
      } else if (typeof value === "number" || typeof value === "boolean" || typeof value === "string") {
        values[path] = value;
      } else if (this.isSpringConfig(value) || this.isEasingConfig(value)) {
        values[path] = value;
      } else if (this.isActionConfig(value)) {
        values[path] = value;
      } else if (this.isSelectConfig(value)) {
        const firstOption = value.options[0];
        const firstValue = typeof firstOption === "string" ? firstOption : firstOption.value;
        values[path] = value.default ?? firstValue;
      } else if (this.isColorConfig(value)) {
        values[path] = value.default ?? "#000000";
      } else if (this.isTextConfig(value)) {
        values[path] = value.default ?? "";
      } else if (this.isUBConfig(value)) {
        values[path] = value.default ?? 0;
      } else if (typeof value === "object" && value !== null) {
        Object.assign(values, this.flattenValues(value, path));
      }
    }
    return values;
  }
  isSpringConfig(value) {
    return typeof value === "object" && value !== null && "type" in value && value.type === "spring";
  }
  isEasingConfig(value) {
    return typeof value === "object" && value !== null && "type" in value && value.type === "easing";
  }
  isActionConfig(value) {
    return typeof value === "object" && value !== null && "type" in value && value.type === "action";
  }
  isSelectConfig(value) {
    return typeof value === "object" && value !== null && "type" in value && value.type === "select" && "options" in value && Array.isArray(value.options);
  }
  isColorConfig(value) {
    return typeof value === "object" && value !== null && "type" in value && value.type === "color";
  }
  isTextConfig(value) {
    return typeof value === "object" && value !== null && "type" in value && value.type === "text";
  }
  isUBConfig(value) {
    return typeof value === "object" && value !== null && "type" in value && typeof value.type === "string" && value.type.startsWith("ub-");
  }
  isHexColor(value) {
    return /^#([0-9A-Fa-f]{3}|[0-9A-Fa-f]{6}|[0-9A-Fa-f]{8})$/.test(value);
  }
  formatLabel(key) {
    return key.replace(/([A-Z])/g, " $1").replace(/^./, (str) => str.toUpperCase()).trim();
  }
  inferRange(value) {
    if (value >= 0 && value <= 1) {
      return { min: 0, max: 1, step: 0.01 };
    } else if (value >= 0 && value <= 10) {
      return { min: 0, max: value * 3 || 10, step: 0.1 };
    } else if (value >= 0 && value <= 100) {
      return { min: 0, max: value * 3 || 100, step: 1 };
    } else if (value >= 0) {
      return { min: 0, max: value * 3 || 1e3, step: 10 };
    } else {
      return { min: value * 3, max: -value * 3, step: 1 };
    }
  }
  inferStep(min, max) {
    const range = max - min;
    if (range <= 1) return 0.01;
    if (range <= 10) return 0.1;
    if (range <= 100) return 1;
    return 10;
  }
  normalizePreservedValue(existingValue, defaultValue, control) {
    if (existingValue === void 0 || !control) {
      return defaultValue;
    }
    switch (control.type) {
      case "slider": {
        if (typeof existingValue !== "number" || typeof defaultValue !== "number") {
          return defaultValue;
        }
        const min = control.min ?? Number.NEGATIVE_INFINITY;
        const max = control.max ?? Number.POSITIVE_INFINITY;
        const clamped = Math.min(max, Math.max(min, existingValue));
        if (typeof control.step !== "number" || control.step <= 0) {
          return clamped;
        }
        return this.roundToStep(clamped, min, max, control.step);
      }
      case "toggle":
        return typeof existingValue === "boolean" ? existingValue : defaultValue;
      case "select": {
        if (typeof existingValue !== "string") {
          return defaultValue;
        }
        const options = control.options ?? [];
        const validValues = new Set(options.map((option) => typeof option === "string" ? option : option.value));
        return validValues.has(existingValue) ? existingValue : defaultValue;
      }
      case "color":
      case "text":
        return typeof existingValue === "string" ? existingValue : defaultValue;
      case "transition":
        if (this.isSpringConfig(defaultValue)) {
          return this.isSpringConfig(existingValue) ? existingValue : defaultValue;
        }
        if (this.isEasingConfig(defaultValue)) {
          return this.isEasingConfig(existingValue) ? existingValue : defaultValue;
        }
        return defaultValue;
      case "action":
        return defaultValue;
      default:
        return defaultValue;
    }
  }
  roundToStep(value, min, max, step) {
    const snapped = min + Math.round((value - min) / step) * step;
    const clamped = Math.min(max, Math.max(min, snapped));
    const precision = this.stepPrecision(step);
    return Number(clamped.toFixed(precision));
  }
  stepPrecision(step) {
    const text = String(step);
    const decimalIndex = text.indexOf(".");
    return decimalIndex === -1 ? 0 : text.length - decimalIndex - 1;
  }
  mapControlsByPath(controls) {
    const map = /* @__PURE__ */ new Map();
    const visit = (nodes) => {
      for (const node of nodes) {
        if (node.type === "folder" && node.children) {
          visit(node.children);
          continue;
        }
        map.set(node.path, node);
      }
    };
    visit(controls);
    return map;
  }
};
var DialStore = new DialStoreClass();
function useDialKit(name, config, options) {
  const instanceId = (0, import_react.useId)();
  const panelId = `${name}-${instanceId}`;
  const configRef = (0, import_react.useRef)(config);
  const serializedConfig = JSON.stringify(config);
  configRef.current = config;
  const onActionRef = (0, import_react.useRef)(options == null ? void 0 : options.onAction);
  onActionRef.current = options == null ? void 0 : options.onAction;
  const shortcutsRef = (0, import_react.useRef)(options == null ? void 0 : options.shortcuts);
  shortcutsRef.current = options == null ? void 0 : options.shortcuts;
  const serializedShortcuts = JSON.stringify(options == null ? void 0 : options.shortcuts);
  (0, import_react.useEffect)(() => {
    DialStore.registerPanel(panelId, name, configRef.current, shortcutsRef.current);
    return () => DialStore.unregisterPanel(panelId);
  }, [panelId, name]);
  const mountedRef = (0, import_react.useRef)(false);
  (0, import_react.useEffect)(() => {
    if (!mountedRef.current) {
      mountedRef.current = true;
      return;
    }
    DialStore.updatePanel(panelId, name, configRef.current, shortcutsRef.current);
  }, [panelId, name, serializedConfig, serializedShortcuts]);
  (0, import_react.useEffect)(() => {
    return DialStore.subscribeActions(panelId, (action) => {
      var _a2;
      (_a2 = onActionRef.current) == null ? void 0 : _a2.call(onActionRef, action);
    });
  }, [panelId]);
  const values = (0, import_react.useSyncExternalStore)(
    (callback) => DialStore.subscribe(panelId, callback),
    () => DialStore.getValues(panelId),
    () => DialStore.getValues(panelId)
  );
  return buildResolvedValues(config, values, "");
}
function buildResolvedValues(config, flatValues, prefix) {
  const result = {};
  for (const [key, configValue] of Object.entries(config)) {
    if (key === "_collapsed") continue;
    const path = prefix ? `${prefix}.${key}` : key;
    if (Array.isArray(configValue) && configValue.length <= 4 && typeof configValue[0] === "number") {
      result[key] = flatValues[path] ?? configValue[0];
    } else if (typeof configValue === "number" || typeof configValue === "boolean" || typeof configValue === "string") {
      result[key] = flatValues[path] ?? configValue;
    } else if (isSpringConfig(configValue) || isEasingConfig(configValue)) {
      result[key] = flatValues[path] ?? configValue;
    } else if (isActionConfig(configValue)) {
      result[key] = flatValues[path] ?? configValue;
    } else if (isSelectConfig(configValue)) {
      const defaultValue = configValue.default ?? getFirstOptionValue(configValue.options);
      result[key] = flatValues[path] ?? defaultValue;
    } else if (isColorConfig(configValue)) {
      result[key] = flatValues[path] ?? configValue.default ?? "#000000";
    } else if (isTextConfig(configValue)) {
      result[key] = flatValues[path] ?? configValue.default ?? "";
    } else if (isUBConfig(configValue)) {
      result[key] = flatValues[path] ?? configValue.default ?? 0;
    } else if (typeof configValue === "object" && configValue !== null) {
      result[key] = buildResolvedValues(configValue, flatValues, path);
    }
  }
  return result;
}
function hasType(value, type) {
  return typeof value === "object" && value !== null && "type" in value && value.type === type;
}
function isSpringConfig(value) {
  return hasType(value, "spring");
}
function isEasingConfig(value) {
  return hasType(value, "easing");
}
function isActionConfig(value) {
  return hasType(value, "action");
}
function isSelectConfig(value) {
  return hasType(value, "select") && "options" in value && Array.isArray(value.options);
}
function isColorConfig(value) {
  return hasType(value, "color");
}
function isTextConfig(value) {
  return hasType(value, "text");
}
function isUBConfig(value) {
  return typeof value === "object" && value !== null && "type" in value && typeof value.type === "string" && value.type.startsWith("ub-");
}
function getFirstOptionValue(options) {
  const first = options[0];
  return typeof first === "string" ? first : first.value;
}
function decimalsForStep(step) {
  const s = step.toString();
  const dot = s.indexOf(".");
  return dot === -1 ? 0 : s.length - dot - 1;
}
function roundValue(val, step) {
  const raw = Math.round(val / step) * step;
  return parseFloat(raw.toFixed(decimalsForStep(step)));
}
function getEffectiveStep(control, shortcut) {
  const min = control.min ?? 0;
  const max = control.max ?? 1;
  const range = max - min;
  const mode = shortcut.mode ?? "normal";
  return mode === "fine" ? range * 0.01 : mode === "coarse" ? range * 0.1 : control.step ?? 1;
}
function applySliderDelta(panelId, path, control, effectiveStep, direction) {
  const currentValue = DialStore.getValue(panelId, path);
  const min = control.min ?? 0;
  const max = control.max ?? 1;
  const newValue = Math.max(min, Math.min(max, currentValue + direction * effectiveStep));
  DialStore.updateValue(panelId, path, roundValue(newValue, effectiveStep));
}
function snapToDecile(rawValue, min, max) {
  const normalized = (rawValue - min) / (max - min);
  const nearest = Math.round(normalized * 10) / 10;
  if (Math.abs(normalized - nearest) <= 0.03125) {
    return min + nearest * (max - min);
  }
  return rawValue;
}
function isInputFocused() {
  const el = document.activeElement;
  if (!el) return false;
  const tag = el.tagName;
  if (tag === "INPUT" || tag === "TEXTAREA") return true;
  if (el.contentEditable === "true") return true;
  return false;
}
function getActiveModifier(e) {
  if (e.altKey) return "alt";
  if (e.shiftKey) return "shift";
  if (e.metaKey) return "meta";
  return void 0;
}
function findControl(controls, path) {
  for (const control of controls) {
    if (control.path === path) return control;
    if (control.type === "folder" && control.children) {
      const found = findControl(control.children, path);
      if (found) return found;
    }
  }
  return null;
}
var DRAG_SENSITIVITY = 4;
function formatInteractionLabel(interaction) {
  switch (interaction) {
    case "drag":
      return "Drag";
    case "move":
      return "Move";
    case "scroll-only":
      return "Scroll";
    default:
      return "Scroll";
  }
}
function formatSliderShortcut(sc) {
  const interaction = sc.interaction ?? "scroll";
  const actionLabel = formatInteractionLabel(interaction);
  if (!sc.key) return actionLabel;
  const mod = formatModifier(sc.modifier);
  return `${mod}${sc.key.toUpperCase()}+${actionLabel}`;
}
function formatToggleShortcut(sc) {
  if (!sc.key) return "Press";
  const mod = formatModifier(sc.modifier);
  return `${mod}${sc.key.toUpperCase()}`;
}
function formatModifier(modifier) {
  return modifier === "alt" ? "⌥" : modifier === "shift" ? "⇧" : modifier === "meta" ? "⌘" : "";
}
var ShortcutContext = (0, import_react5.createContext)({ activePanelId: null, activePath: null });
function ShortcutListener({ children }) {
  const [activeShortcut, setActiveShortcut] = (0, import_react5.useState)({ activePanelId: null, activePath: null });
  const activeKeysRef = (0, import_react5.useRef)(/* @__PURE__ */ new Set());
  const isDraggingRef = (0, import_react5.useRef)(false);
  const lastMouseXRef = (0, import_react5.useRef)(null);
  const dragAccumulatorRef = (0, import_react5.useRef)(0);
  const resolveActiveTarget = (0, import_react5.useCallback)((interaction) => {
    for (const key of activeKeysRef.current) {
      const panels = DialStore.getPanels();
      for (const panel of panels) {
        for (const [path, shortcut] of Object.entries(panel.shortcuts)) {
          if (!shortcut.key) continue;
          if (shortcut.key.toLowerCase() !== key) continue;
          if ((shortcut.interaction ?? "scroll") !== interaction) continue;
          const control = findControl(panel.controls, path);
          if (control && control.type === "slider") {
            return { panelId: panel.id, path, control, shortcut };
          }
        }
      }
    }
    return null;
  }, []);
  (0, import_react5.useEffect)(() => {
    const handleKeyDown = (e) => {
      if (isInputFocused()) return;
      const key = e.key.toLowerCase();
      if (key === "arrowleft" || key === "arrowright" || key === "arrowup" || key === "arrowdown") {
        if (activeKeysRef.current.size > 0) {
          const target2 = resolveActiveTarget("scroll") || resolveActiveTarget("drag") || resolveActiveTarget("move");
          if (target2 && target2.control.type === "slider") {
            e.preventDefault();
            const direction = key === "arrowright" || key === "arrowup" ? 1 : -1;
            const effectiveStep = getEffectiveStep(target2.control, target2.shortcut);
            applySliderDelta(target2.panelId, target2.path, target2.control, effectiveStep, direction);
            return;
          }
        }
      }
      const wasAlreadyHeld = activeKeysRef.current.has(key);
      activeKeysRef.current.add(key);
      const modifier = getActiveModifier(e);
      const target = DialStore.resolveShortcutTarget(key, modifier);
      if (target) {
        setActiveShortcut({ activePanelId: target.panelId, activePath: target.path });
        if (!wasAlreadyHeld && target.control.type === "toggle") {
          const currentValue = DialStore.getValue(target.panelId, target.path);
          DialStore.updateValue(target.panelId, target.path, !currentValue);
        }
      }
      if (!wasAlreadyHeld) {
        lastMouseXRef.current = null;
        dragAccumulatorRef.current = 0;
      }
    };
    const handleKeyUp = (e) => {
      const key = e.key.toLowerCase();
      activeKeysRef.current.delete(key);
      isDraggingRef.current = false;
      lastMouseXRef.current = null;
      dragAccumulatorRef.current = 0;
      if (activeKeysRef.current.size === 0) {
        setActiveShortcut({ activePanelId: null, activePath: null });
      } else {
        let found = false;
        for (const remainingKey of activeKeysRef.current) {
          const modifier = getActiveModifier(e);
          const target = DialStore.resolveShortcutTarget(remainingKey, modifier);
          if (target) {
            setActiveShortcut({ activePanelId: target.panelId, activePath: target.path });
            found = true;
            break;
          }
        }
        if (!found) {
          setActiveShortcut({ activePanelId: null, activePath: null });
        }
      }
    };
    const handleWheel = (e) => {
      var _a2;
      if (isInputFocused()) return;
      const modifier = getActiveModifier(e);
      if (activeKeysRef.current.size > 0) {
        for (const key of activeKeysRef.current) {
          const target = DialStore.resolveShortcutTarget(key, modifier);
          if (!target) continue;
          const { panelId, path, control } = target;
          const interaction = ((_a2 = control.shortcut) == null ? void 0 : _a2.interaction) ?? "scroll";
          if (interaction !== "scroll" || control.type !== "slider") continue;
          e.preventDefault();
          const effectiveStep = getEffectiveStep(control, control.shortcut);
          const direction = e.deltaY > 0 ? -1 : 1;
          applySliderDelta(panelId, path, control, effectiveStep, direction);
          return;
        }
      }
      const scrollOnlyTargets = DialStore.resolveScrollOnlyTargets();
      for (const { panelId, path, control, shortcut } of scrollOnlyTargets) {
        if (control.type !== "slider") continue;
        e.preventDefault();
        const effectiveStep = getEffectiveStep(control, shortcut);
        const direction = e.deltaY > 0 ? -1 : 1;
        applySliderDelta(panelId, path, control, effectiveStep, direction);
        return;
      }
    };
    const handleMouseDown = (e) => {
      if (isInputFocused()) return;
      if (activeKeysRef.current.size === 0) return;
      const target = resolveActiveTarget("drag");
      if (target) {
        isDraggingRef.current = true;
        lastMouseXRef.current = e.clientX;
        dragAccumulatorRef.current = 0;
        e.preventDefault();
      }
    };
    const handleMouseUp = () => {
      isDraggingRef.current = false;
      lastMouseXRef.current = null;
      dragAccumulatorRef.current = 0;
    };
    const handleMouseMove = (e) => {
      if (isInputFocused()) return;
      if (activeKeysRef.current.size === 0) return;
      if (isDraggingRef.current) {
        const target = resolveActiveTarget("drag");
        if (target && lastMouseXRef.current !== null) {
          const deltaX = e.clientX - lastMouseXRef.current;
          lastMouseXRef.current = e.clientX;
          dragAccumulatorRef.current += deltaX;
          const effectiveStep = getEffectiveStep(target.control, target.shortcut);
          const steps = Math.trunc(dragAccumulatorRef.current / DRAG_SENSITIVITY);
          if (steps !== 0) {
            dragAccumulatorRef.current -= steps * DRAG_SENSITIVITY;
            applySliderDelta(target.panelId, target.path, target.control, effectiveStep, steps);
          }
        }
        return;
      }
      const moveTarget = resolveActiveTarget("move");
      if (moveTarget) {
        if (lastMouseXRef.current === null) {
          lastMouseXRef.current = e.clientX;
          return;
        }
        const deltaX = e.clientX - lastMouseXRef.current;
        lastMouseXRef.current = e.clientX;
        dragAccumulatorRef.current += deltaX;
        const effectiveStep = getEffectiveStep(moveTarget.control, moveTarget.shortcut);
        const steps = Math.trunc(dragAccumulatorRef.current / DRAG_SENSITIVITY);
        if (steps !== 0) {
          dragAccumulatorRef.current -= steps * DRAG_SENSITIVITY;
          applySliderDelta(moveTarget.panelId, moveTarget.path, moveTarget.control, effectiveStep, steps);
        }
      }
    };
    const handleWindowBlur = () => {
      activeKeysRef.current.clear();
      isDraggingRef.current = false;
      lastMouseXRef.current = null;
      dragAccumulatorRef.current = 0;
      setActiveShortcut({ activePanelId: null, activePath: null });
    };
    window.addEventListener("keydown", handleKeyDown);
    window.addEventListener("keyup", handleKeyUp);
    window.addEventListener("wheel", handleWheel, { passive: false });
    window.addEventListener("mousedown", handleMouseDown);
    window.addEventListener("mouseup", handleMouseUp);
    window.addEventListener("mousemove", handleMouseMove);
    window.addEventListener("blur", handleWindowBlur);
    return () => {
      window.removeEventListener("keydown", handleKeyDown);
      window.removeEventListener("keyup", handleKeyUp);
      window.removeEventListener("wheel", handleWheel);
      window.removeEventListener("mousedown", handleMouseDown);
      window.removeEventListener("mouseup", handleMouseUp);
      window.removeEventListener("mousemove", handleMouseMove);
      window.removeEventListener("blur", handleWindowBlur);
    };
  }, [resolveActiveTarget]);
  return (0, import_jsx_runtime.jsx)(ShortcutContext.Provider, { value: activeShortcut, children });
}
var ICON_CHEVRON = "M6 9.5L12 15.5L18 9.5";
var ICON_CHECK = "M5 12.75L10 19L19 5";
var ICON_CLIPBOARD = {
  board: "M8 6C8 4.34315 9.34315 3 11 3H13C14.6569 3 16 4.34315 16 6V7H8V6Z",
  sparkle: "M19.2405 16.1852L18.5436 14.3733C18.4571 14.1484 18.241 14 18 14C17.759 14 17.5429 14.1484 17.4564 14.3733L16.7595 16.1852C16.658 16.4493 16.4493 16.658 16.1852 16.7595L14.3733 17.4564C14.1484 17.5429 14 17.759 14 18C14 18.241 14.1484 18.4571 14.3733 18.5436L16.1852 19.2405C16.4493 19.342 16.658 19.5507 16.7595 19.8148L17.4564 21.6267C17.5429 21.8516 17.759 22 18 22C18.241 22 18.4571 21.8516 18.5436 21.6267L19.2405 19.8148C19.342 19.5507 19.5507 19.342 19.8148 19.2405L21.6267 18.5436C21.8516 18.4571 22 18.241 22 18C22 17.759 21.8516 17.5429 21.6267 17.4564L19.8148 16.7595C19.5507 16.658 19.342 16.4493 19.2405 16.1852Z",
  body: "M16 5H17C18.6569 5 20 6.34315 20 8V11M8 5H7C5.34315 5 4 6.34315 4 8V18C4 19.6569 5.34315 21 7 21H12"
};
var ICON_ADD_PRESET = [
  "M4 6H20",
  "M4 12H10",
  "M15 15L21 15",
  "M18 12V18",
  "M4 18H10"
];
var ICON_TRASH = [
  "M5 6.5L5.80734 18.2064C5.91582 19.7794 7.22348 21 8.80023 21H15.1998C16.7765 21 18.0842 19.7794 18.1927 18.2064L19 6.5",
  "M10 11V16",
  "M14 11V16",
  "M3.5 6H20.5",
  "M8.07092 5.74621C8.42348 3.89745 10.0485 2.5 12 2.5C13.9515 2.5 15.5765 3.89745 15.9291 5.74621"
];
var ICON_PANEL = {
  path: "M6.84766 11.75C6.78583 11.9899 6.75 12.2408 6.75 12.5C6.75 12.7592 6.78583 13.0101 6.84766 13.25H2C1.58579 13.25 1.25 12.9142 1.25 12.5C1.25 12.0858 1.58579 11.75 2 11.75H6.84766ZM14 11.75C14.4142 11.75 14.75 12.0858 14.75 12.5C14.75 12.9142 14.4142 13.25 14 13.25H12.6523C12.7142 13.0101 12.75 12.7592 12.75 12.5C12.75 12.2408 12.7142 11.9899 12.6523 11.75H14ZM3.09766 7.25C3.03583 7.48994 3 7.74075 3 8C3 8.25925 3.03583 8.51006 3.09766 8.75H2C1.58579 8.75 1.25 8.41421 1.25 8C1.25 7.58579 1.58579 7.25 2 7.25H3.09766ZM14 7.25C14.4142 7.25 14.75 7.58579 14.75 8C14.75 8.41421 14.4142 8.75 14 8.75H8.90234C8.96417 8.51006 9 8.25925 9 8C9 7.74075 8.96417 7.48994 8.90234 7.25H14ZM7.59766 2.75C7.53583 2.98994 7.5 3.24075 7.5 3.5C7.5 3.75925 7.53583 4.01006 7.59766 4.25H2C1.58579 4.25 1.25 3.91421 1.25 3.5C1.25 3.08579 1.58579 2.75 2 2.75H7.59766ZM14 2.75C14.4142 2.75 14.75 3.08579 14.75 3.5C14.75 3.91421 14.4142 4.25 14 4.25H13.4023C13.4642 4.01006 13.5 3.75925 13.5 3.5C13.5 3.24075 13.4642 2.98994 13.4023 2.75H14Z",
  circles: [
    { cx: "6", cy: "8", r: "0.998596" },
    { cx: "10.4999", cy: "3.5", r: "0.998657" },
    { cx: "9.75015", cy: "12.5", r: "0.997986" }
  ]
};
function Folder({ title, children, defaultOpen = true, isRoot = false, inline = false, onOpenChange, toolbar }) {
  const [isOpen, setIsOpen] = (0, import_react6.useState)(defaultOpen);
  const [isCollapsed, setIsCollapsed] = (0, import_react6.useState)(!defaultOpen);
  const contentRef = (0, import_react6.useRef)(null);
  const [contentHeight, setContentHeight] = (0, import_react6.useState)(void 0);
  const [windowHeight, setWindowHeight] = (0, import_react6.useState)(typeof window !== "undefined" ? window.innerHeight : 800);
  (0, import_react6.useEffect)(() => {
    if (!isRoot) return;
    const onResize = () => setWindowHeight(window.innerHeight);
    window.addEventListener("resize", onResize);
    return () => window.removeEventListener("resize", onResize);
  }, [isRoot]);
  (0, import_react6.useEffect)(() => {
    const el = contentRef.current;
    if (!el) return;
    const ro = new ResizeObserver(() => {
      if (isOpen) {
        const h = el.offsetHeight;
        setContentHeight((prev) => prev === h ? prev : h);
      }
    });
    ro.observe(el);
    return () => ro.disconnect();
  }, [isOpen]);
  const handleToggle = () => {
    if (inline && isRoot) return;
    const next = !isOpen;
    setIsOpen(next);
    if (next) {
      setIsCollapsed(false);
    } else {
      setIsCollapsed(true);
    }
    onOpenChange == null ? void 0 : onOpenChange(next);
  };
  const folderContent = (0, import_jsx_runtime2.jsxs)("div", { ref: isRoot ? contentRef : void 0, className: `dialkit-folder ${isRoot ? "dialkit-folder-root" : ""}`, children: [
    (0, import_jsx_runtime2.jsxs)("div", { className: `dialkit-folder-header ${isRoot ? "dialkit-panel-header" : ""}`, onClick: handleToggle, children: [
      (0, import_jsx_runtime2.jsxs)("div", { className: "dialkit-folder-header-top", children: [
        isRoot ? isOpen && (0, import_jsx_runtime2.jsx)("div", { className: "dialkit-folder-title-row", children: (0, import_jsx_runtime2.jsx)("span", { className: "dialkit-folder-title dialkit-folder-title-root", children: title }) }) : (0, import_jsx_runtime2.jsx)("div", { className: "dialkit-folder-title-row", children: (0, import_jsx_runtime2.jsx)("span", { className: "dialkit-folder-title", children: title }) }),
        isRoot && !inline && (0, import_jsx_runtime2.jsxs)(
          "svg",
          {
            className: "dialkit-panel-icon",
            viewBox: "0 0 16 16",
            fill: "none",
            children: [
              (0, import_jsx_runtime2.jsx)("path", { opacity: "0.5", d: ICON_PANEL.path, fill: "currentColor" }),
              ICON_PANEL.circles.map((c, i) => (0, import_jsx_runtime2.jsx)("circle", { cx: c.cx, cy: c.cy, r: c.r, fill: "currentColor", stroke: "currentColor", strokeWidth: "1.25" }, i))
            ]
          }
        ),
        !isRoot && (0, import_jsx_runtime2.jsx)(
          motion.svg,
          {
            className: "dialkit-folder-icon",
            viewBox: "0 0 24 24",
            fill: "none",
            stroke: "currentColor",
            strokeWidth: "2.5",
            strokeLinecap: "round",
            strokeLinejoin: "round",
            initial: false,
            animate: { rotate: isOpen ? 0 : 180 },
            transition: { type: "spring", visualDuration: 0.35, bounce: 0.15 },
            children: (0, import_jsx_runtime2.jsx)("path", { d: ICON_CHEVRON })
          }
        )
      ] }),
      isRoot && toolbar && isOpen && (0, import_jsx_runtime2.jsx)("div", { className: "dialkit-panel-toolbar", onClick: (e) => e.stopPropagation(), children: toolbar })
    ] }),
    (0, import_jsx_runtime2.jsx)(AnimatePresence, { initial: false, children: isOpen && (0, import_jsx_runtime2.jsx)(
      motion.div,
      {
        className: "dialkit-folder-content",
        initial: isRoot ? void 0 : { height: 0, opacity: 0 },
        animate: isRoot ? void 0 : { height: "auto", opacity: 1 },
        exit: isRoot ? void 0 : { height: 0, opacity: 0 },
        transition: isRoot ? void 0 : { type: "spring", visualDuration: 0.35, bounce: 0.1 },
        style: isRoot ? void 0 : { clipPath: "inset(0 -20px)" },
        children: (0, import_jsx_runtime2.jsx)("div", { className: "dialkit-folder-inner", children })
      }
    ) })
  ] });
  if (isRoot) {
    if (inline) {
      return (0, import_jsx_runtime2.jsx)("div", { className: "dialkit-panel-inner dialkit-panel-inline", children: folderContent });
    }
    const panelStyle = isOpen ? { width: 280, height: contentHeight !== void 0 ? Math.min(contentHeight + 10, windowHeight - 32) : "auto", borderRadius: 14, boxShadow: "var(--dial-shadow)", cursor: void 0, overflowY: "auto" } : { width: 42, height: 42, borderRadius: "50%", boxSizing: "border-box", boxShadow: "var(--dial-shadow-collapsed)", overflow: "hidden", cursor: "pointer" };
    return (0, import_jsx_runtime2.jsx)(
      motion.div,
      {
        className: "dialkit-panel-inner",
        style: panelStyle,
        onClick: !isOpen ? handleToggle : void 0,
        "data-collapsed": isCollapsed,
        whileTap: !isOpen ? { scale: 0.9 } : void 0,
        transition: { type: "spring", visualDuration: 0.15, bounce: 0.3 },
        children: folderContent
      }
    );
  }
  return folderContent;
}
var CLICK_THRESHOLD = 3;
var DEAD_ZONE = 32;
var MAX_CURSOR_RANGE = 200;
var MAX_STRETCH = 8;
function Slider({
  label,
  value,
  onChange,
  min = 0,
  max = 1,
  step = 0.01,
  unit,
  shortcut,
  shortcutActive
}) {
  var _a2;
  const wrapperRef = (0, import_react8.useRef)(null);
  const trackRef = (0, import_react8.useRef)(null);
  const inputRef = (0, import_react8.useRef)(null);
  const labelRef = (0, import_react8.useRef)(null);
  const valueSpanRef = (0, import_react8.useRef)(null);
  const [isInteracting, setIsInteracting] = (0, import_react8.useState)(false);
  const [isDragging, setIsDragging] = (0, import_react8.useState)(false);
  const [isHovered, setIsHovered] = (0, import_react8.useState)(false);
  const [isValueHovered, setIsValueHovered] = (0, import_react8.useState)(false);
  const [isValueEditable, setIsValueEditable] = (0, import_react8.useState)(false);
  const [showInput, setShowInput] = (0, import_react8.useState)(false);
  const [inputValue, setInputValue] = (0, import_react8.useState)("");
  const hoverTimeoutRef = (0, import_react8.useRef)(null);
  const pointerDownPos = (0, import_react8.useRef)(null);
  const isClickRef = (0, import_react8.useRef)(true);
  const animRef = (0, import_react8.useRef)(null);
  const wrapperRectRef = (0, import_react8.useRef)(null);
  const scaleRef = (0, import_react8.useRef)(1);
  const percentage = (value - min) / (max - min) * 100;
  const isActive = isInteracting || isHovered;
  const fillPercent = useMotionValue(percentage);
  const fillWidth = useTransform(fillPercent, (pct) => `${pct}%`);
  const handleLeft = useTransform(
    fillPercent,
    (pct) => `max(5px, calc(${pct}% - 9px))`
  );
  const rubberStretchPx = useMotionValue(0);
  const rubberBandWidth = useTransform(
    rubberStretchPx,
    (stretch) => `calc(100% + ${Math.abs(stretch)}px)`
  );
  const rubberBandX = useTransform(
    rubberStretchPx,
    (stretch) => stretch < 0 ? stretch : 0
  );
  (0, import_react8.useEffect)(() => {
    if (!isInteracting && !animRef.current) {
      fillPercent.jump(percentage);
    }
  }, [percentage, isInteracting, fillPercent]);
  const positionToValue = (0, import_react8.useCallback)(
    (clientX) => {
      const rect = wrapperRectRef.current;
      if (!rect) return value;
      const screenX = clientX - rect.left;
      const sceneX = screenX / scaleRef.current;
      const nativeWidth = wrapperRef.current ? wrapperRef.current.offsetWidth : rect.width;
      const percent = Math.max(0, Math.min(1, sceneX / nativeWidth));
      const rawValue = min + percent * (max - min);
      return Math.max(min, Math.min(max, rawValue));
    },
    [min, max, value]
  );
  const percentFromValue = (0, import_react8.useCallback)(
    (v) => (v - min) / (max - min) * 100,
    [min, max]
  );
  const computeRubberStretch = (0, import_react8.useCallback)(
    (clientX, sign) => {
      const rect = wrapperRectRef.current;
      if (!rect) return 0;
      const distancePast = sign < 0 ? rect.left - clientX : clientX - rect.right;
      const overflow = Math.max(0, distancePast - DEAD_ZONE);
      return sign * MAX_STRETCH * Math.sqrt(Math.min(overflow / MAX_CURSOR_RANGE, 1));
    },
    []
  );
  const handlePointerDown = (0, import_react8.useCallback)(
    (e) => {
      if (showInput) return;
      e.preventDefault();
      e.target.setPointerCapture(e.pointerId);
      pointerDownPos.current = { x: e.clientX, y: e.clientY };
      isClickRef.current = true;
      setIsInteracting(true);
      if (wrapperRef.current) {
        wrapperRectRef.current = wrapperRef.current.getBoundingClientRect();
        const nativeWidth = wrapperRef.current.offsetWidth;
        scaleRef.current = wrapperRectRef.current.width / nativeWidth;
      }
    },
    [showInput]
  );
  const handlePointerMove = (0, import_react8.useCallback)(
    (e) => {
      if (!isInteracting || !pointerDownPos.current) return;
      const dx = e.clientX - pointerDownPos.current.x;
      const dy = e.clientY - pointerDownPos.current.y;
      const distance = Math.sqrt(dx * dx + dy * dy);
      if (isClickRef.current && distance > CLICK_THRESHOLD) {
        isClickRef.current = false;
        setIsDragging(true);
      }
      if (!isClickRef.current) {
        const rect = wrapperRectRef.current;
        if (rect) {
          if (e.clientX < rect.left) {
            rubberStretchPx.jump(computeRubberStretch(e.clientX, -1));
          } else if (e.clientX > rect.right) {
            rubberStretchPx.jump(computeRubberStretch(e.clientX, 1));
          } else {
            rubberStretchPx.jump(0);
          }
        }
        const newValue = positionToValue(e.clientX);
        const newPct = percentFromValue(newValue);
        if (animRef.current) {
          animRef.current.stop();
          animRef.current = null;
        }
        fillPercent.jump(newPct);
        onChange(roundValue(newValue, step));
      }
    },
    [
      isInteracting,
      positionToValue,
      percentFromValue,
      onChange,
      fillPercent,
      rubberStretchPx,
      computeRubberStretch
    ]
  );
  const handlePointerUp = (0, import_react8.useCallback)(
    (e) => {
      if (!isInteracting) return;
      if (isClickRef.current) {
        const rawValue = positionToValue(e.clientX);
        const discreteSteps2 = (max - min) / step;
        const snappedValue = discreteSteps2 <= 10 ? Math.max(min, Math.min(max, min + Math.round((rawValue - min) / step) * step)) : snapToDecile(rawValue, min, max);
        const newPct = percentFromValue(snappedValue);
        if (animRef.current) {
          animRef.current.stop();
        }
        animRef.current = animate(fillPercent, newPct, {
          type: "spring",
          stiffness: 300,
          damping: 25,
          mass: 0.8,
          onComplete: () => {
            animRef.current = null;
          }
        });
        onChange(roundValue(snappedValue, step));
      }
      if (rubberStretchPx.get() !== 0) {
        animate(rubberStretchPx, 0, {
          type: "spring",
          visualDuration: 0.35,
          bounce: 0.15
        });
      }
      setIsInteracting(false);
      setIsDragging(false);
      pointerDownPos.current = null;
    },
    [
      isInteracting,
      positionToValue,
      percentFromValue,
      onChange,
      min,
      max,
      fillPercent,
      rubberStretchPx
    ]
  );
  (0, import_react8.useEffect)(() => {
    if (isValueHovered && !showInput && !isValueEditable) {
      hoverTimeoutRef.current = setTimeout(() => {
        setIsValueEditable(true);
      }, 800);
    } else if (!isValueHovered && !showInput) {
      if (hoverTimeoutRef.current) {
        clearTimeout(hoverTimeoutRef.current);
        hoverTimeoutRef.current = null;
      }
      setIsValueEditable(false);
    }
    return () => {
      if (hoverTimeoutRef.current) {
        clearTimeout(hoverTimeoutRef.current);
      }
    };
  }, [isValueHovered, showInput, isValueEditable]);
  (0, import_react8.useEffect)(() => {
    if (showInput && inputRef.current) {
      inputRef.current.focus();
      inputRef.current.select();
    }
  }, [showInput]);
  const handleInputChange = (e) => {
    setInputValue(e.target.value);
  };
  const handleInputSubmit = () => {
    const parsed = parseFloat(inputValue);
    if (!isNaN(parsed)) {
      const clamped = Math.max(min, Math.min(max, parsed));
      onChange(roundValue(clamped, step));
    }
    setShowInput(false);
    setIsValueHovered(false);
    setIsValueEditable(false);
  };
  const handleValueClick = (e) => {
    if (isValueEditable) {
      e.stopPropagation();
      e.preventDefault();
      setShowInput(true);
      setInputValue(value.toFixed(decimalsForStep(step)));
    }
  };
  const handleInputKeyDown = (e) => {
    if (e.key === "Enter") {
      handleInputSubmit();
    } else if (e.key === "Escape") {
      setShowInput(false);
      setIsValueHovered(false);
    }
  };
  const handleInputBlur = () => {
    handleInputSubmit();
  };
  const displayValue = value.toFixed(decimalsForStep(step));
  const HANDLE_BUFFER = 8;
  const LABEL_CSS_LEFT = 10;
  const VALUE_CSS_RIGHT = 10;
  let leftThreshold = 30;
  let rightThreshold = 78;
  const trackWidth = (_a2 = wrapperRef.current) == null ? void 0 : _a2.offsetWidth;
  if (trackWidth && trackWidth > 0) {
    if (labelRef.current) {
      leftThreshold = (LABEL_CSS_LEFT + labelRef.current.offsetWidth + HANDLE_BUFFER) / trackWidth * 100;
    }
    if (valueSpanRef.current) {
      rightThreshold = (trackWidth - VALUE_CSS_RIGHT - valueSpanRef.current.offsetWidth - HANDLE_BUFFER) / trackWidth * 100;
    }
  }
  const valueDodge = percentage < leftThreshold || percentage > rightThreshold;
  const handleOpacity = !isActive ? 0 : valueDodge ? 0.1 : isDragging ? 0.9 : 0.5;
  const discreteSteps = (max - min) / step;
  const hashMarks = discreteSteps <= 10 ? Array.from({ length: discreteSteps - 1 }, (_, i) => {
    const pct = (i + 1) * step / (max - min) * 100;
    return (0, import_jsx_runtime3.jsx)(
      "div",
      {
        className: "dialkit-slider-hashmark",
        style: { left: `${pct}%` }
      },
      i
    );
  }) : Array.from({ length: 9 }, (_, i) => {
    const pct = (i + 1) * 10;
    return (0, import_jsx_runtime3.jsx)(
      "div",
      {
        className: "dialkit-slider-hashmark",
        style: { left: `${pct}%` }
      },
      i
    );
  });
  return (0, import_jsx_runtime3.jsx)("div", { ref: wrapperRef, className: "dialkit-slider-wrapper", children: (0, import_jsx_runtime3.jsxs)(
    motion.div,
    {
      ref: trackRef,
      className: `dialkit-slider ${isActive ? "dialkit-slider-active" : ""}`,
      onPointerDown: handlePointerDown,
      onPointerMove: handlePointerMove,
      onPointerUp: handlePointerUp,
      onMouseEnter: () => setIsHovered(true),
      onMouseLeave: () => setIsHovered(false),
      style: { width: rubberBandWidth, x: rubberBandX },
      children: [
        (0, import_jsx_runtime3.jsx)("div", { className: "dialkit-slider-hashmarks", children: hashMarks }),
        (0, import_jsx_runtime3.jsx)(
          motion.div,
          {
            className: "dialkit-slider-fill",
            style: {
              width: fillWidth
            }
          }
        ),
        (0, import_jsx_runtime3.jsx)(
          motion.div,
          {
            className: "dialkit-slider-handle",
            style: {
              left: handleLeft,
              y: "-50%"
            },
            animate: {
              opacity: handleOpacity,
              scaleX: isActive ? 1 : 0.25,
              scaleY: isActive && valueDodge ? 0.75 : 1
            },
            transition: {
              scaleX: { type: "spring", visualDuration: 0.25, bounce: 0.15 },
              scaleY: { type: "spring", visualDuration: 0.2, bounce: 0.1 },
              opacity: { duration: 0.15 }
            }
          }
        ),
        (0, import_jsx_runtime3.jsxs)("span", { ref: labelRef, className: "dialkit-slider-label", children: [
          label,
          shortcut && (0, import_jsx_runtime3.jsx)("span", { className: `dialkit-shortcut-pill${shortcutActive ? " dialkit-shortcut-pill-active" : ""}`, children: formatSliderShortcut(shortcut) })
        ] }),
        showInput ? (0, import_jsx_runtime3.jsx)(
          "input",
          {
            ref: inputRef,
            type: "text",
            className: "dialkit-slider-input",
            value: inputValue,
            onChange: handleInputChange,
            onKeyDown: handleInputKeyDown,
            onBlur: handleInputBlur,
            onClick: (e) => e.stopPropagation(),
            onMouseDown: (e) => e.stopPropagation()
          }
        ) : (0, import_jsx_runtime3.jsx)(
          "span",
          {
            ref: valueSpanRef,
            className: `dialkit-slider-value ${isValueEditable ? "dialkit-slider-value-editable" : ""}`,
            onMouseEnter: () => setIsValueHovered(true),
            onMouseLeave: () => setIsValueHovered(false),
            onClick: handleValueClick,
            onMouseDown: (e) => isValueEditable && e.stopPropagation(),
            style: { cursor: isValueEditable ? "text" : "default" },
            children: displayValue
          }
        )
      ]
    }
  ) });
}
function SegmentedControl({
  options,
  value,
  onChange
}) {
  const containerRef = (0, import_react10.useRef)(null);
  const hasAnimated = (0, import_react10.useRef)(false);
  const [pillStyle, setPillStyle] = (0, import_react10.useState)(null);
  const measure = (0, import_react10.useCallback)(() => {
    const container = containerRef.current;
    if (!container) return;
    const activeButton = container.querySelector('[data-active="true"]');
    if (!activeButton) return;
    setPillStyle({
      left: activeButton.offsetLeft,
      width: activeButton.offsetWidth
    });
  }, []);
  (0, import_react10.useLayoutEffect)(() => {
    measure();
  }, [value, options.length, measure]);
  const shouldAnimate = hasAnimated.current;
  hasAnimated.current = true;
  return (0, import_jsx_runtime4.jsxs)("div", { className: "dialkit-segmented", ref: containerRef, children: [
    pillStyle && (0, import_jsx_runtime4.jsx)(
      "div",
      {
        className: "dialkit-segmented-pill",
        style: {
          left: pillStyle.left,
          width: pillStyle.width,
          transition: shouldAnimate ? "left 0.2s cubic-bezier(0.25, 1, 0.5, 1), width 0.2s cubic-bezier(0.25, 1, 0.5, 1)" : "none"
        }
      }
    ),
    options.map((option) => {
      const isActive = value === option.value;
      return (0, import_jsx_runtime4.jsx)(
        "button",
        {
          onClick: () => onChange(option.value),
          className: "dialkit-segmented-button",
          "data-active": String(isActive),
          children: option.label
        },
        option.value
      );
    })
  ] });
}
function Toggle({ label, checked, onChange, shortcut, shortcutActive }) {
  return (0, import_jsx_runtime5.jsxs)("div", { className: "dialkit-labeled-control", children: [
    (0, import_jsx_runtime5.jsxs)("span", { className: "dialkit-labeled-control-label", children: [
      label,
      shortcut && (0, import_jsx_runtime5.jsx)("span", { className: `dialkit-shortcut-pill${shortcutActive ? " dialkit-shortcut-pill-active" : ""}`, children: formatToggleShortcut(shortcut) })
    ] }),
    (0, import_jsx_runtime5.jsx)(
      SegmentedControl,
      {
        options: [
          { value: "off", label: "Off" },
          { value: "on", label: "On" }
        ],
        value: checked ? "on" : "off",
        onChange: (val) => onChange(val === "on")
      }
    )
  ] });
}
function generateSpringCurve(stiffness, damping, mass, duration) {
  const points = [];
  const steps = 100;
  const dt = duration / steps;
  let position = 0;
  let velocity = 0;
  const target = 1;
  for (let i = 0; i <= steps; i++) {
    const time = i * dt;
    points.push([time, position]);
    const springForce = -stiffness * (position - target);
    const dampingForce = -damping * velocity;
    const acceleration = (springForce + dampingForce) / mass;
    velocity += acceleration * dt;
    position += velocity * dt;
  }
  return points;
}
function SpringVisualization({ spring, isSimpleMode }) {
  const width = 256;
  const height = 140;
  let stiffness;
  let damping;
  let mass;
  if (isSimpleMode) {
    const visualDuration = spring.visualDuration ?? 0.3;
    const bounce = spring.bounce ?? 0.2;
    mass = 1;
    stiffness = 2 * Math.PI / visualDuration;
    stiffness = Math.pow(stiffness, 2);
    const dampingRatio = 1 - bounce;
    damping = 2 * dampingRatio * Math.sqrt(stiffness * mass);
  } else {
    stiffness = spring.stiffness ?? 400;
    damping = spring.damping ?? 17;
    mass = spring.mass ?? 1;
  }
  const duration = 2;
  const points = generateSpringCurve(stiffness, damping, mass, duration);
  const values = points.map(([, value]) => value);
  const minValue = Math.min(...values);
  const maxValue = Math.max(...values);
  const valueRange = maxValue - minValue;
  const pathData = points.map(([time, value], i) => {
    const x = time / duration * width;
    const normalizedValue = (value - minValue) / (valueRange || 1);
    const y = height - (normalizedValue * height * 0.6 + height * 0.2);
    return `${i === 0 ? "M" : "L"} ${x} ${y}`;
  }).join(" ");
  const gridLines = [];
  for (let i = 1; i < 4; i++) {
    const x = width / 4 * i;
    const y = height / 4 * i;
    gridLines.push(
      (0, import_jsx_runtime6.jsx)("line", { x1: x, y1: 0, x2: x, y2: height, stroke: "rgba(255, 255, 255, 0.08)", strokeWidth: "1" }, `v-${i}`),
      (0, import_jsx_runtime6.jsx)("line", { x1: 0, y1: y, x2: width, y2: y, stroke: "rgba(255, 255, 255, 0.08)", strokeWidth: "1" }, `h-${i}`)
    );
  }
  return (0, import_jsx_runtime6.jsxs)("svg", { viewBox: `0 0 ${width} ${height}`, className: "dialkit-spring-viz", children: [
    gridLines,
    (0, import_jsx_runtime6.jsx)(
      "line",
      {
        x1: 0,
        y1: height / 2,
        x2: width,
        y2: height / 2,
        stroke: "rgba(255, 255, 255, 0.15)",
        strokeWidth: "1",
        strokeDasharray: "4,4"
      }
    ),
    (0, import_jsx_runtime6.jsx)(
      "path",
      {
        d: pathData,
        fill: "none",
        stroke: "rgba(255, 255, 255, 0.6)",
        strokeWidth: "2",
        strokeLinecap: "round",
        strokeLinejoin: "round"
      }
    )
  ] });
}
function SpringControl({ panelId, path, label, spring, onChange }) {
  const mode = (0, import_react11.useSyncExternalStore)(
    (cb) => DialStore.subscribe(panelId, cb),
    () => DialStore.getSpringMode(panelId, path),
    () => DialStore.getSpringMode(panelId, path)
  );
  const isSimpleMode = mode === "simple";
  const cache = (0, import_react11.useRef)({
    simple: spring.visualDuration !== void 0 ? spring : { type: "spring", visualDuration: 0.3, bounce: 0.2 },
    advanced: spring.stiffness !== void 0 ? spring : { type: "spring", stiffness: 200, damping: 25, mass: 1 }
  });
  if (isSimpleMode) {
    cache.current.simple = spring;
  } else {
    cache.current.advanced = spring;
  }
  const handleModeChange = (newMode) => {
    DialStore.updateSpringMode(panelId, path, newMode);
    if (newMode === "simple") {
      onChange(cache.current.simple);
    } else {
      onChange(cache.current.advanced);
    }
  };
  const handleUpdate = (key, value) => {
    if (isSimpleMode) {
      const { stiffness, damping, mass, ...rest } = spring;
      onChange({ ...rest, [key]: value });
    } else {
      const { visualDuration, bounce, ...rest } = spring;
      onChange({ ...rest, [key]: value });
    }
  };
  return (0, import_jsx_runtime7.jsx)(Folder, { title: label, defaultOpen: true, children: (0, import_jsx_runtime7.jsxs)("div", { style: { display: "flex", flexDirection: "column", gap: 6 }, children: [
    (0, import_jsx_runtime7.jsx)(SpringVisualization, { spring, isSimpleMode }),
    (0, import_jsx_runtime7.jsxs)("div", { className: "dialkit-labeled-control", children: [
      (0, import_jsx_runtime7.jsx)("span", { className: "dialkit-labeled-control-label", children: "Type" }),
      (0, import_jsx_runtime7.jsx)(
        SegmentedControl,
        {
          options: [
            { value: "simple", label: "Time" },
            { value: "advanced", label: "Physics" }
          ],
          value: mode,
          onChange: handleModeChange
        }
      )
    ] }),
    isSimpleMode ? (0, import_jsx_runtime7.jsxs)(import_jsx_runtime7.Fragment, { children: [
      (0, import_jsx_runtime7.jsx)(
        Slider,
        {
          label: "Duration",
          value: spring.visualDuration ?? 0.3,
          onChange: (v) => handleUpdate("visualDuration", v),
          min: 0.1,
          max: 1,
          step: 0.05,
          unit: "s"
        }
      ),
      (0, import_jsx_runtime7.jsx)(
        Slider,
        {
          label: "Bounce",
          value: spring.bounce ?? 0.2,
          onChange: (v) => handleUpdate("bounce", v),
          min: 0,
          max: 1,
          step: 0.05
        }
      )
    ] }) : (0, import_jsx_runtime7.jsxs)(import_jsx_runtime7.Fragment, { children: [
      (0, import_jsx_runtime7.jsx)(
        Slider,
        {
          label: "Stiffness",
          value: spring.stiffness ?? 400,
          onChange: (v) => handleUpdate("stiffness", v),
          min: 1,
          max: 1e3,
          step: 10
        }
      ),
      (0, import_jsx_runtime7.jsx)(
        Slider,
        {
          label: "Damping",
          value: spring.damping ?? 17,
          onChange: (v) => handleUpdate("damping", v),
          min: 1,
          max: 100,
          step: 1
        }
      ),
      (0, import_jsx_runtime7.jsx)(
        Slider,
        {
          label: "Mass",
          value: spring.mass ?? 1,
          onChange: (v) => handleUpdate("mass", v),
          min: 0.1,
          max: 10,
          step: 0.1
        }
      )
    ] })
  ] }) });
}
function EasingVisualization({ easing }) {
  const ease = easing.ease;
  const s = 200;
  const pad = 10;
  const inner = s - pad * 2;
  const unit = inner / 2;
  const toSvg = (nx, ny) => ({
    x: pad + (nx + 0.5) * unit,
    y: pad + (1.5 - ny) * unit
  });
  const start = toSvg(0, 0);
  const end = toSvg(1, 1);
  const p1 = toSvg(ease[0], ease[1]);
  const p2 = toSvg(ease[2], ease[3]);
  const curvePath = `M ${start.x} ${start.y} C ${p1.x} ${p1.y}, ${p2.x} ${p2.y}, ${end.x} ${end.y}`;
  return (0, import_jsx_runtime8.jsxs)(
    "svg",
    {
      viewBox: `0 0 ${s} ${s}`,
      preserveAspectRatio: "xMidYMid slice",
      className: "dialkit-spring-viz dialkit-easing-viz",
      children: [
        (0, import_jsx_runtime8.jsx)(
          "line",
          {
            x1: start.x,
            y1: start.y,
            x2: end.x,
            y2: end.y,
            stroke: "rgba(255, 255, 255, 0.15)",
            strokeWidth: "1",
            strokeDasharray: "4,4"
          }
        ),
        (0, import_jsx_runtime8.jsx)("path", { d: curvePath, fill: "none", stroke: "rgba(255, 255, 255, 0.6)", strokeWidth: "2", strokeLinecap: "round" })
      ]
    }
  );
}
function TransitionControl({ panelId, path, label, value, onChange }) {
  const mode = (0, import_react12.useSyncExternalStore)(
    (cb) => DialStore.subscribe(panelId, cb),
    () => DialStore.getTransitionMode(panelId, path),
    () => DialStore.getTransitionMode(panelId, path)
  );
  const isEasing = mode === "easing";
  const isSimpleSpring = mode === "simple";
  const cache = (0, import_react12.useRef)({
    easing: value.type === "easing" ? value : { type: "easing", duration: 0.3, ease: [1, -0.4, 0.5, 1] },
    simple: value.type === "spring" && value.visualDuration !== void 0 ? value : { type: "spring", visualDuration: 0.3, bounce: 0.2 },
    advanced: value.type === "spring" && value.stiffness !== void 0 ? value : { type: "spring", stiffness: 200, damping: 25, mass: 1 }
  });
  if (isEasing && value.type === "easing") {
    cache.current.easing = value;
  } else if (isSimpleSpring && value.type === "spring") {
    cache.current.simple = value;
  } else if (mode === "advanced" && value.type === "spring") {
    cache.current.advanced = value;
  }
  const spring = value.type === "spring" ? value : cache.current.simple;
  const easing = value.type === "easing" ? value : cache.current.easing;
  const handleModeChange = (newMode) => {
    DialStore.updateTransitionMode(panelId, path, newMode);
    if (newMode === "easing") {
      onChange(cache.current.easing);
    } else if (newMode === "simple") {
      onChange(cache.current.simple);
    } else {
      onChange(cache.current.advanced);
    }
  };
  const handleSpringUpdate = (key, val) => {
    if (isSimpleSpring) {
      const { stiffness, damping, mass, ...rest } = spring;
      onChange({ ...rest, [key]: val });
    } else {
      const { visualDuration, bounce, ...rest } = spring;
      onChange({ ...rest, [key]: val });
    }
  };
  const updateEase = (index, val) => {
    const newEase = [...easing.ease];
    newEase[index] = val;
    onChange({ ...easing, ease: newEase });
  };
  return (0, import_jsx_runtime9.jsx)(Folder, { title: label, defaultOpen: true, children: (0, import_jsx_runtime9.jsxs)("div", { style: { display: "flex", flexDirection: "column", gap: 6 }, children: [
    isEasing ? (0, import_jsx_runtime9.jsx)(EasingVisualization, { easing }) : (0, import_jsx_runtime9.jsx)(SpringVisualization, { spring, isSimpleMode: isSimpleSpring }),
    (0, import_jsx_runtime9.jsxs)("div", { className: "dialkit-labeled-control", children: [
      (0, import_jsx_runtime9.jsx)("span", { className: "dialkit-labeled-control-label", children: "Type" }),
      (0, import_jsx_runtime9.jsx)(
        SegmentedControl,
        {
          options: [
            { value: "easing", label: "Easing" },
            { value: "simple", label: "Time" },
            { value: "advanced", label: "Physics" }
          ],
          value: mode,
          onChange: handleModeChange
        }
      )
    ] }),
    isEasing ? (0, import_jsx_runtime9.jsxs)(import_jsx_runtime9.Fragment, { children: [
      (0, import_jsx_runtime9.jsx)(Slider, { label: "x1", value: easing.ease[0], onChange: (v) => updateEase(0, v), min: 0, max: 1, step: 0.01 }),
      (0, import_jsx_runtime9.jsx)(Slider, { label: "y1", value: easing.ease[1], onChange: (v) => updateEase(1, v), min: -1, max: 2, step: 0.01 }),
      (0, import_jsx_runtime9.jsx)(Slider, { label: "x2", value: easing.ease[2], onChange: (v) => updateEase(2, v), min: 0, max: 1, step: 0.01 }),
      (0, import_jsx_runtime9.jsx)(Slider, { label: "y2", value: easing.ease[3], onChange: (v) => updateEase(3, v), min: -1, max: 2, step: 0.01 }),
      (0, import_jsx_runtime9.jsx)(Slider, { label: "Duration", value: easing.duration, onChange: (v) => onChange({ ...easing, duration: v }), min: 0.1, max: 2, step: 0.05, unit: "s" }),
      (0, import_jsx_runtime9.jsx)(EaseTextInput, { ease: easing.ease, onChange: (newEase) => onChange({ ...easing, ease: newEase }) })
    ] }) : isSimpleSpring ? (0, import_jsx_runtime9.jsxs)(import_jsx_runtime9.Fragment, { children: [
      (0, import_jsx_runtime9.jsx)(Slider, { label: "Duration", value: spring.visualDuration ?? 0.3, onChange: (v) => handleSpringUpdate("visualDuration", v), min: 0.1, max: 1, step: 0.05, unit: "s" }),
      (0, import_jsx_runtime9.jsx)(Slider, { label: "Bounce", value: spring.bounce ?? 0.2, onChange: (v) => handleSpringUpdate("bounce", v), min: 0, max: 1, step: 0.05 })
    ] }) : (0, import_jsx_runtime9.jsxs)(import_jsx_runtime9.Fragment, { children: [
      (0, import_jsx_runtime9.jsx)(Slider, { label: "Stiffness", value: spring.stiffness ?? 400, onChange: (v) => handleSpringUpdate("stiffness", v), min: 1, max: 1e3, step: 10 }),
      (0, import_jsx_runtime9.jsx)(Slider, { label: "Damping", value: spring.damping ?? 17, onChange: (v) => handleSpringUpdate("damping", v), min: 1, max: 100, step: 1 }),
      (0, import_jsx_runtime9.jsx)(Slider, { label: "Mass", value: spring.mass ?? 1, onChange: (v) => handleSpringUpdate("mass", v), min: 0.1, max: 10, step: 0.1 })
    ] })
  ] }) });
}
function formatEase(ease) {
  return ease.map((v) => parseFloat(v.toFixed(2))).join(", ");
}
function parseEase(str) {
  const parts = str.split(",").map((s) => parseFloat(s.trim()));
  if (parts.length === 4 && parts.every((n) => !isNaN(n))) {
    return parts;
  }
  return null;
}
function EaseTextInput({ ease, onChange }) {
  const [editing, setEditing] = (0, import_react12.useState)(false);
  const [draft, setDraft] = (0, import_react12.useState)("");
  const handleFocus = () => {
    setDraft(formatEase(ease));
    setEditing(true);
  };
  const handleBlur = () => {
    const parsed = parseEase(draft);
    if (parsed) onChange(parsed);
    setEditing(false);
  };
  const handleKeyDown = (e) => {
    if (e.key === "Enter") {
      e.target.blur();
    }
  };
  return (0, import_jsx_runtime9.jsxs)("div", { className: "dialkit-labeled-control", children: [
    (0, import_jsx_runtime9.jsx)("span", { className: "dialkit-labeled-control-label", children: "Ease" }),
    (0, import_jsx_runtime9.jsx)(
      "input",
      {
        type: "text",
        className: "dialkit-text-input",
        value: editing ? draft : formatEase(ease),
        onChange: (e) => setDraft(e.target.value),
        onFocus: handleFocus,
        onBlur: handleBlur,
        onKeyDown: handleKeyDown,
        spellCheck: false
      }
    )
  ] });
}
function TextControl({ label, value, onChange, placeholder }) {
  return (0, import_jsx_runtime10.jsxs)("div", { className: "dialkit-text-control", children: [
    (0, import_jsx_runtime10.jsx)("label", { className: "dialkit-text-label", children: label }),
    (0, import_jsx_runtime10.jsx)(
      "input",
      {
        type: "text",
        className: "dialkit-text-input",
        value,
        onChange: (e) => onChange(e.target.value),
        placeholder
      }
    )
  ] });
}
function toTitleCase(s) {
  return s.replace(/\b\w/g, (c) => c.toUpperCase());
}
function normalizeOptions(options) {
  return options.map(
    (opt) => typeof opt === "string" ? { value: opt, label: toTitleCase(opt) } : opt
  );
}
function SelectControl({ label, value, options, onChange }) {
  const [isOpen, setIsOpen] = (0, import_react13.useState)(false);
  const triggerRef = (0, import_react13.useRef)(null);
  const dropdownRef = (0, import_react13.useRef)(null);
  const [portalTarget, setPortalTarget] = (0, import_react13.useState)(null);
  const [pos, setPos] = (0, import_react13.useState)(null);
  const normalized = normalizeOptions(options);
  const selectedOption = normalized.find((o) => o.value === value);
  const updatePos = (0, import_react13.useCallback)(() => {
    const el = triggerRef.current;
    if (!el) return;
    const rect = el.getBoundingClientRect();
    const dropdownHeight = 8 + normalized.length * 36;
    const spaceBelow = window.innerHeight - rect.bottom - 4;
    const above = spaceBelow < dropdownHeight && rect.top > spaceBelow;
    setPos({
      top: above ? rect.top - 4 : rect.bottom + 4,
      left: rect.left,
      width: rect.width,
      above
    });
  }, [normalized.length]);
  (0, import_react13.useEffect)(() => {
    var _a2;
    const root = (_a2 = triggerRef.current) == null ? void 0 : _a2.closest(".dialkit-root");
    setPortalTarget(root ?? document.body);
  }, []);
  (0, import_react13.useEffect)(() => {
    if (!isOpen) return;
    updatePos();
  }, [isOpen, updatePos]);
  (0, import_react13.useEffect)(() => {
    if (!isOpen) return;
    const handleClick = (e) => {
      const target = e.target;
      if (triggerRef.current && !triggerRef.current.contains(target) && dropdownRef.current && !dropdownRef.current.contains(target)) {
        setIsOpen(false);
      }
    };
    document.addEventListener("mousedown", handleClick);
    return () => document.removeEventListener("mousedown", handleClick);
  }, [isOpen]);
  return (0, import_jsx_runtime11.jsxs)("div", { className: "dialkit-select-row", children: [
    (0, import_jsx_runtime11.jsxs)(
      "button",
      {
        ref: triggerRef,
        className: "dialkit-select-trigger",
        onClick: () => setIsOpen(!isOpen),
        "data-open": String(isOpen),
        children: [
          (0, import_jsx_runtime11.jsx)("span", { className: "dialkit-select-label", children: label }),
          (0, import_jsx_runtime11.jsxs)("div", { className: "dialkit-select-right", children: [
            (0, import_jsx_runtime11.jsx)("span", { className: "dialkit-select-value", children: (selectedOption == null ? void 0 : selectedOption.label) ?? value }),
            (0, import_jsx_runtime11.jsx)(
              motion.svg,
              {
                className: "dialkit-select-chevron",
                viewBox: "0 0 24 24",
                fill: "none",
                stroke: "currentColor",
                strokeWidth: "2.5",
                strokeLinecap: "round",
                strokeLinejoin: "round",
                animate: { rotate: isOpen ? 180 : 0 },
                transition: { type: "spring", visualDuration: 0.2, bounce: 0.15 },
                children: (0, import_jsx_runtime11.jsx)("path", { d: ICON_CHEVRON })
              }
            )
          ] })
        ]
      }
    ),
    portalTarget && (0, import_react_dom2.createPortal)(
      (0, import_jsx_runtime11.jsx)(AnimatePresence, { children: isOpen && pos && (0, import_jsx_runtime11.jsx)(
        motion.div,
        {
          ref: dropdownRef,
          className: "dialkit-select-dropdown",
          initial: { opacity: 0, y: pos.above ? 8 : -8, scale: 0.95 },
          animate: { opacity: 1, y: 0, scale: 1 },
          exit: { opacity: 0, y: pos.above ? 8 : -8, scale: 0.95 },
          transition: { type: "spring", visualDuration: 0.15, bounce: 0 },
          style: {
            position: "fixed",
            left: pos.left,
            width: pos.width,
            ...pos.above ? { bottom: window.innerHeight - pos.top, transformOrigin: "bottom" } : { top: pos.top, transformOrigin: "top" }
          },
          children: normalized.map((option) => (0, import_jsx_runtime11.jsx)(
            "button",
            {
              className: "dialkit-select-option",
              "data-selected": String(option.value === value),
              onClick: () => {
                onChange(option.value);
                setIsOpen(false);
              },
              children: option.label
            },
            option.value
          ))
        }
      ) }),
      portalTarget
    )
  ] });
}
var HEX_COLOR_REGEX = /^#([0-9A-Fa-f]{3}|[0-9A-Fa-f]{6}|[0-9A-Fa-f]{8})$/;
function ColorControl({ label, value, onChange }) {
  const [isEditing, setIsEditing] = (0, import_react15.useState)(false);
  const [editValue, setEditValue] = (0, import_react15.useState)(value);
  const colorInputRef = (0, import_react15.useRef)(null);
  (0, import_react15.useEffect)(() => {
    if (!isEditing) {
      setEditValue(value);
    }
  }, [value, isEditing]);
  function handleTextSubmit() {
    setIsEditing(false);
    if (HEX_COLOR_REGEX.test(editValue)) {
      onChange(editValue);
    } else {
      setEditValue(value);
    }
  }
  function handleKeyDown(e) {
    if (e.key === "Enter") {
      handleTextSubmit();
    } else if (e.key === "Escape") {
      setIsEditing(false);
      setEditValue(value);
    }
  }
  return (0, import_jsx_runtime12.jsxs)("div", { className: "dialkit-color-control", children: [
    (0, import_jsx_runtime12.jsx)("span", { className: "dialkit-color-label", children: label }),
    (0, import_jsx_runtime12.jsxs)("div", { className: "dialkit-color-inputs", children: [
      isEditing ? (0, import_jsx_runtime12.jsx)(
        "input",
        {
          type: "text",
          className: "dialkit-color-hex-input",
          value: editValue,
          onChange: (e) => setEditValue(e.target.value),
          onBlur: handleTextSubmit,
          onKeyDown: handleKeyDown,
          autoFocus: true
        }
      ) : (0, import_jsx_runtime12.jsx)(
        "span",
        {
          className: "dialkit-color-hex",
          onClick: () => setIsEditing(true),
          children: (value ?? "").toUpperCase()
        }
      ),
      (0, import_jsx_runtime12.jsx)(
        "button",
        {
          className: "dialkit-color-swatch",
          style: { backgroundColor: value },
          onClick: () => {
            var _a2;
            return (_a2 = colorInputRef.current) == null ? void 0 : _a2.click();
          },
          title: "Pick color"
        }
      ),
      (0, import_jsx_runtime12.jsx)(
        "input",
        {
          ref: colorInputRef,
          type: "color",
          className: "dialkit-color-picker-native",
          value: value.length === 4 ? expandShorthandHex(value) : value.slice(0, 7),
          onChange: (e) => onChange(e.target.value)
        }
      )
    ] })
  ] });
}
function expandShorthandHex(hex) {
  if (hex.length !== 4) return hex;
  return `#${hex[1]}${hex[1]}${hex[2]}${hex[2]}${hex[3]}${hex[3]}`;
}
function PresetManager({ panelId, presets, activePresetId, onAdd }) {
  const [isOpen, setIsOpen] = (0, import_react16.useState)(false);
  const triggerRef = (0, import_react16.useRef)(null);
  const dropdownRef = (0, import_react16.useRef)(null);
  const [pos, setPos] = (0, import_react16.useState)({ top: 0, left: 0, width: 0 });
  const hasPresets = presets.length > 0;
  const activePreset = presets.find((p) => p.id === activePresetId);
  const open = (0, import_react16.useCallback)(() => {
    var _a2;
    if (!hasPresets) return;
    const rect = (_a2 = triggerRef.current) == null ? void 0 : _a2.getBoundingClientRect();
    if (rect) {
      setPos({ top: rect.bottom + 4, left: rect.left, width: rect.width });
    }
    setIsOpen(true);
  }, [hasPresets]);
  const close = (0, import_react16.useCallback)(() => setIsOpen(false), []);
  const toggle = (0, import_react16.useCallback)(() => {
    if (isOpen) close();
    else open();
  }, [isOpen, open, close]);
  (0, import_react16.useEffect)(() => {
    if (!isOpen) return;
    const handler = (e) => {
      var _a2, _b;
      const target = e.target;
      if (((_a2 = triggerRef.current) == null ? void 0 : _a2.contains(target)) || ((_b = dropdownRef.current) == null ? void 0 : _b.contains(target))) return;
      close();
    };
    document.addEventListener("mousedown", handler);
    return () => document.removeEventListener("mousedown", handler);
  }, [isOpen, close]);
  const handleSelect = (presetId) => {
    if (presetId) {
      DialStore.loadPreset(panelId, presetId);
    } else {
      DialStore.clearActivePreset(panelId);
    }
    close();
  };
  const handleDelete = (e, presetId) => {
    e.stopPropagation();
    DialStore.deletePreset(panelId, presetId);
  };
  return (0, import_jsx_runtime13.jsxs)("div", { className: "dialkit-preset-manager", children: [
    (0, import_jsx_runtime13.jsxs)(
      "button",
      {
        ref: triggerRef,
        className: "dialkit-preset-trigger",
        onClick: toggle,
        "data-open": String(isOpen),
        "data-has-preset": String(!!activePreset),
        "data-disabled": String(!hasPresets),
        children: [
          (0, import_jsx_runtime13.jsx)("span", { className: "dialkit-preset-label", children: activePreset ? activePreset.name : "Default" }),
          (0, import_jsx_runtime13.jsx)(
            motion.svg,
            {
              className: "dialkit-select-chevron",
              viewBox: "0 0 24 24",
              fill: "none",
              stroke: "currentColor",
              strokeWidth: "2.5",
              strokeLinecap: "round",
              strokeLinejoin: "round",
              animate: { rotate: isOpen ? 180 : 0, opacity: hasPresets ? 0.6 : 0.25 },
              transition: { type: "spring", visualDuration: 0.2, bounce: 0.15 },
              children: (0, import_jsx_runtime13.jsx)("path", { d: ICON_CHEVRON })
            }
          )
        ]
      }
    ),
    (0, import_react_dom3.createPortal)(
      (0, import_jsx_runtime13.jsx)(AnimatePresence, { children: isOpen && (0, import_jsx_runtime13.jsxs)(
        motion.div,
        {
          ref: dropdownRef,
          className: "dialkit-root dialkit-preset-dropdown",
          style: { position: "fixed", top: pos.top, left: pos.left, minWidth: pos.width },
          initial: { opacity: 0, y: 4, scale: 0.97 },
          animate: { opacity: 1, y: 0, scale: 1 },
          exit: { opacity: 0, y: 4, scale: 0.97, pointerEvents: "none" },
          transition: { type: "spring", visualDuration: 0.15, bounce: 0 },
          children: [
            // Hide the "Default" fallback entry when seeded presets exist —
            // experiments that seed presets already expose a named baseline
            // (e.g., "Clap"), so "Default" is redundant and confusing.
            presets.length === 0 && (0, import_jsx_runtime13.jsx)(
              "div",
              {
                className: "dialkit-preset-item",
                "data-active": String(!activePresetId),
                onClick: () => handleSelect(null),
                children: (0, import_jsx_runtime13.jsx)("span", { className: "dialkit-preset-name", children: "Default" })
              }
            ),
            presets.map((preset) => (0, import_jsx_runtime13.jsxs)(
              "div",
              {
                className: "dialkit-preset-item",
                "data-active": String(preset.id === activePresetId),
                onClick: () => handleSelect(preset.id),
                children: [
                  (0, import_jsx_runtime13.jsx)("span", { className: "dialkit-preset-name", children: preset.name }),
                  (0, import_jsx_runtime13.jsx)(
                    "button",
                    {
                      className: "dialkit-preset-delete",
                      onClick: (e) => handleDelete(e, preset.id),
                      title: "Delete preset",
                      children: (0, import_jsx_runtime13.jsx)("svg", { viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: ICON_TRASH.map((d, i) => (0, import_jsx_runtime13.jsx)("path", { d }, i)) })
                    }
                  )
                ]
              },
              preset.id
            ))
          ]
        }
      ) }),
      document.body
    )
  ] });
}
var UB_DEFAULTS = {
  // Thresholds
  clickThreshold: 3,
  // Rubber band
  deadZone: 32,
  maxCursorRange: 200,
  maxStretch: 8,
  springDuration: 0.35,
  springBounce: 0.15,
  // Auto-scroll zone
  speedMultiplier: 2,
  speedExponent: 1.5,
  speedDivisor: 50,
  maxSpeed: 100,
  // Drag physics
  quadraticExponent: 2,
  dragScale: 0.5,
  // Handle opacity states
  handleRestOpacity: 0.3,
  handleHoverOpacity: 0.5,
  handleDragOpacity: 0.9,
  handleDodgeOpacity: 0.1,
  handleDodgeScaleY: 0.75,
  // Handle animation springs
  scaleXDuration: 0.25,
  scaleXBounce: 0.15,
  scaleYDuration: 0.2,
  scaleYBounce: 0.1,
  opacityDuration: 0.15,
  // Edit
  editHoverDelay: 800,
  // Toggle
  bias: 0.1
};
function UBControl({ variant, label, value, step, onChange }) {
  var _a2, _b;
  var T = typeof window !== "undefined" && window.__UB_TUNING__ || {};
  var cfg = { ...UB_DEFAULTS, ...T };
  const dec = (s) => {
    const t = s.toString(), d = t.indexOf(".");
    return d === -1 ? 0 : t.length - d - 1;
  };
  const rnd = (v) => Number.parseFloat((Math.round(v / step) * step).toFixed(dec(step)));
  const display = value.toFixed(dec(step));
  const [editing, setEditing] = (0, import_react18.useState)(false);
  const [inputVal, setInputVal] = (0, import_react18.useState)("");
  const [hovered, setHovered] = (0, import_react18.useState)(false);
  const [dragging, setDragging] = (0, import_react18.useState)(false);
  const [isInteracting, setIsInteracting] = (0, import_react18.useState)(false);
  const [isValueHovered, setIsValueHovered] = (0, import_react18.useState)(false);
  const [isValueEditable, setIsValueEditable] = (0, import_react18.useState)(false);
  const inputRef = (0, import_react18.useRef)(null);
  const wrapperRef = (0, import_react18.useRef)(null);
  const labelRef = (0, import_react18.useRef)(null);
  const valueRef = (0, import_react18.useRef)(null);
  const rowRef = (0, import_react18.useRef)(null);
  const dragState = (0, import_react18.useRef)({ active: false, startX: 0, startVal: 0, accDx: 0 });
  const pointerDownPos = (0, import_react18.useRef)(null);
  const isClickRef = (0, import_react18.useRef)(true);
  const wrapperRectRef = (0, import_react18.useRef)(null);
  const scaleRef = (0, import_react18.useRef)(1);
  const hoverTimeoutRef = (0, import_react18.useRef)(null);
  const isActive = isInteracting || hovered;
  (0, import_react18.useEffect)(() => {
    if (isValueHovered && !editing && !isValueEditable) {
      hoverTimeoutRef.current = setTimeout(() => {
        setIsValueEditable(true);
      }, cfg.editHoverDelay);
    } else if (!isValueHovered && !editing) {
      if (hoverTimeoutRef.current) {
        clearTimeout(hoverTimeoutRef.current);
        hoverTimeoutRef.current = null;
      }
      setIsValueEditable(false);
    }
    return () => {
      if (hoverTimeoutRef.current) clearTimeout(hoverTimeoutRef.current);
    };
  }, [isValueHovered, editing, isValueEditable]);
  (0, import_react18.useEffect)(() => {
    if (editing && inputRef.current) {
      inputRef.current.focus();
      inputRef.current.select();
    }
  }, [editing]);
  const startEdit = () => {
    setEditing(true);
    setInputVal(display);
  };
  const submitEdit = () => {
    const p = parseFloat(inputVal);
    if (!isNaN(p)) onChange(rnd(p));
    setEditing(false);
    setIsValueHovered(false);
    setIsValueEditable(false);
  };
  const handleValueClick = (e) => {
    if (isValueEditable) {
      e.stopPropagation();
      e.preventDefault();
      setEditing(true);
      setInputVal(display);
    }
  };
  const mkInput = () => (0, import_jsx_runtime14.jsx)("input", { ref: inputRef, type: "text", className: "dialkit-slider-input", value: inputVal, onChange: (e) => setInputVal(e.target.value), onKeyDown: (e) => {
    if (e.key === "Enter") submitEdit();
    else if (e.key === "Escape") {
      setEditing(false);
      setIsValueHovered(false);
    }
    e.stopPropagation();
  }, onBlur: submitEdit, onClick: (e) => e.stopPropagation(), onMouseDown: (e) => e.stopPropagation() });
  const mkValue = () => (0, import_jsx_runtime14.jsx)("span", {
    ref: valueRef,
    className: `dialkit-slider-value ${isValueEditable ? "dialkit-slider-value-editable" : ""}`,
    onMouseEnter: () => setIsValueHovered(true),
    onMouseLeave: () => setIsValueHovered(false),
    onClick: handleValueClick,
    onMouseDown: (e) => isValueEditable && e.stopPropagation(),
    onPointerDown: (e) => isValueEditable && e.stopPropagation(),
    style: { cursor: isValueEditable ? "text" : "default" },
    children: display
  });
  const rubberStretchPx = useMotionValue(0);
  const rubberBandWidth = useTransform(rubberStretchPx, (stretch) => `calc(100% + ${Math.abs(stretch)}px)`);
  const rubberBandX = useTransform(rubberStretchPx, (stretch) => stretch < 0 ? stretch : 0);
  const computeRubberStretch = (0, import_react18.useCallback)((clientX, sign) => {
    const rect = wrapperRectRef.current;
    if (!rect) return 0;
    const distancePast = sign < 0 ? rect.left - clientX : clientX - rect.right;
    const overflow = Math.max(0, distancePast - cfg.deadZone);
    return sign * cfg.maxStretch * Math.sqrt(Math.min(overflow / cfg.maxCursorRange, 1));
  }, []);
  const computeDodge = (handlePct) => {
    var _a3, _b2, _c;
    const trackW = (_a3 = wrapperRef.current) == null ? void 0 : _a3.offsetWidth;
    if (!trackW || trackW <= 0) {
      return handlePct < 30 || handlePct > 78;
    }
    const labelW = ((_b2 = labelRef.current) == null ? void 0 : _b2.offsetWidth) ?? 0;
    const valueW = ((_c = valueRef.current) == null ? void 0 : _c.offsetWidth) ?? 0;
    const leftThresh = (10 + labelW + 8) / trackW * 100;
    const rightThresh = (trackW - 10 - valueW - 8) / trackW * 100;
    return handlePct < leftThresh || handlePct > rightThresh;
  };
  const springDots = [-0.75, -0.5, -0.25, 0.25, 0.5, 0.75];
  if (variant === "ub-1" || variant === "ub-4" || variant === "ub-7" || variant === "ub-8") {
    const alwaysShow = variant === "ub-4" || variant === "ub-8";
    const hasAutoScroll = variant === "ub-7" || variant === "ub-8";
    const hasMomentum = variant === "ub-4";
    const [handleX, setHandleX] = (0, import_react18.useState)(0);
    const [autoDir, setAutoDir] = (0, import_react18.useState)(0);
    const autoRef = (0, import_react18.useRef)(0);
    const autoSpeedRef = (0, import_react18.useRef)(0);
    const velRef = (0, import_react18.useRef)(0);
    const frameRef = (0, import_react18.useRef)(0);
    const valRef = (0, import_react18.useRef)(value);
    valRef.current = value;
    const trackW = ((_a2 = wrapperRef.current) == null ? void 0 : _a2.offsetWidth) || 200;
    const handlePct = 50 + handleX / trackW * 100;
    const dodge = computeDodge(handlePct);
    const handleOpacity = !isActive && !alwaysShow ? 0 : dodge ? cfg.handleDodgeOpacity : dragging ? cfg.handleDragOpacity : alwaysShow ? cfg.handleRestOpacity : cfg.handleHoverOpacity;
    const startAutoScrollLoop = (dir) => {
      autoRef.current = dir;
      setAutoDir(dir);
      let last = performance.now();
      const loop = (now) => {
        if (!dragState.current.active || autoRef.current === 0) return;
        const dt = Math.min((now - last) / 1e3, 0.1);
        last = now;
        const speed = Math.min(autoSpeedRef.current, step * cfg.maxSpeed);
        valRef.current = rnd(valRef.current + speed * autoRef.current * dt * 60);
        onChange(valRef.current);
        frameRef.current = requestAnimationFrame(loop);
      };
      frameRef.current = requestAnimationFrame(loop);
    };
    const stopAutoScroll = () => {
      if (autoRef.current !== 0) {
        autoRef.current = 0;
        setAutoDir(0);
        cancelAnimationFrame(frameRef.current);
      }
    };
    const onDown = (e) => {
      var _a3;
      if (editing) return;
      e.preventDefault();
      e.target.setPointerCapture(e.pointerId);
      pointerDownPos.current = { x: e.clientX, y: e.clientY };
      isClickRef.current = true;
      setIsInteracting(true);
      cancelAnimationFrame(frameRef.current);
      if (wrapperRef.current) {
        wrapperRectRef.current = wrapperRef.current.getBoundingClientRect();
        scaleRef.current = wrapperRectRef.current.width / wrapperRef.current.offsetWidth;
      }
      const rect = (_a3 = wrapperRef.current) == null ? void 0 : _a3.getBoundingClientRect();
      const cx = rect ? rect.left + rect.width / 2 : 0;
      const hw = rect ? rect.width / 2 : 100;
      dragState.current = { active: true, startX: cx, startVal: value, accDx: hw };
      velRef.current = 0;
      stopAutoScroll();
      autoSpeedRef.current = 0;
    };
    const onMove = (e) => {
      if (!isInteracting || !pointerDownPos.current) return;
      const dx = e.clientX - pointerDownPos.current.x;
      const dy = e.clientY - pointerDownPos.current.y;
      const distance = Math.sqrt(dx * dx + dy * dy);
      if (isClickRef.current && distance > cfg.clickThreshold) {
        isClickRef.current = false;
        setDragging(true);
      }
      if (!isClickRef.current) {
        const rect = wrapperRectRef.current;
        const cx = dragState.current.startX, hw = dragState.current.accDx;
        const offset = Math.max(-hw, Math.min(hw, e.clientX - cx));
        setHandleX(offset);
        velRef.current = e.movementX;
        const pastLeft = rect && e.clientX < rect.left;
        const pastRight = rect && e.clientX > rect.right;
        const pastEdge = pastLeft || pastRight;
        if (hasAutoScroll && pastEdge && rect) {
          if (pastLeft) rubberStretchPx.jump(computeRubberStretch(e.clientX, -1));
          else rubberStretchPx.jump(computeRubberStretch(e.clientX, 1));
          const dir = pastLeft ? -1 : 1;
          const distPast = dir < 0 ? rect.left - e.clientX : e.clientX - rect.right;
          autoSpeedRef.current = step * Math.pow(distPast / cfg.speedDivisor, cfg.speedExponent) * cfg.speedMultiplier;
          if (autoRef.current === 0) {
            startAutoScrollLoop(dir);
          } else if (autoRef.current !== dir) {
            stopAutoScroll();
            startAutoScrollLoop(dir);
          }
        } else {
          if (autoRef.current !== 0) {
            stopAutoScroll();
            if (rubberStretchPx.get() !== 0) {
              animate(rubberStretchPx, 0, { type: "spring", visualDuration: cfg.springDuration, bounce: cfg.springBounce });
            }
          } else if (rect) {
            rubberStretchPx.jump(0);
          }
          const norm = offset / hw;
          const absNorm = Math.abs(norm);
          const accel = Math.sign(norm) * Math.pow(absNorm, cfg.quadraticExponent);
          const nv = rnd(dragState.current.startVal + accel * step * hw * cfg.dragScale);
          onChange(nv);
          valRef.current = nv;
        }
      }
    };
    const onUp = () => {
      if (!isInteracting) return;
      if (rubberStretchPx.get() !== 0) {
        animate(rubberStretchPx, 0, { type: "spring", visualDuration: cfg.springDuration, bounce: cfg.springBounce });
      }
      dragState.current.active = false;
      setIsInteracting(false);
      setDragging(false);
      pointerDownPos.current = null;
      stopAutoScroll();
      if (hasMomentum && !isClickRef.current) {
        let vel = velRef.current * step * 2;
        let hx = handleX;
        const coast = () => {
          vel *= 0.92;
          hx *= 0.88;
          setHandleX(hx);
          valRef.current = rnd(valRef.current + vel);
          onChange(valRef.current);
          if (Math.abs(vel) > step * 0.01) frameRef.current = requestAnimationFrame(coast);
          else setHandleX(0);
        };
        if (Math.abs(vel) > step * 0.1) frameRef.current = requestAnimationFrame(coast);
        else setHandleX(0);
      } else {
        setHandleX(0);
      }
    };
    return (0, import_jsx_runtime14.jsx)("div", { ref: wrapperRef, className: "dialkit-slider-wrapper", children: (0, import_jsx_runtime14.jsxs)(
      motion.div,
      {
        className: `ub-spring ${isActive ? "ub-spring--active" : ""}`,
        onPointerDown: onDown,
        onPointerMove: onMove,
        onPointerUp: onUp,
        onPointerCancel: onUp,
        onMouseEnter: () => setHovered(true),
        onMouseLeave: () => setHovered(false),
        style: { width: rubberBandWidth, x: rubberBandX, cursor: autoDir !== 0 ? autoDir > 0 ? "e-resize" : "w-resize" : void 0 },
        children: [
          (0, import_jsx_runtime14.jsx)("div", { className: "ub-spring__notch" }),
          springDots.map((d, i) => (0, import_jsx_runtime14.jsx)("div", { className: "ub-spring__dot", style: { left: `${50 + d * 50}%` } }, i)),
          (0, import_jsx_runtime14.jsx)(motion.div, {
            className: "ub-spring__handle",
            style: { left: `max(5px, min(calc(100% - 8px), calc(50% + ${handleX}px - 1.5px)))`, y: "-50%" },
            animate: {
              opacity: handleOpacity,
              scaleX: isActive || alwaysShow ? 1 : 0.25,
              scaleY: isActive && dodge ? cfg.handleDodgeScaleY : 1
            },
            transition: {
              scaleX: { type: "spring", visualDuration: cfg.scaleXDuration, bounce: cfg.scaleXBounce },
              scaleY: { type: "spring", visualDuration: cfg.scaleYDuration, bounce: cfg.scaleYBounce },
              opacity: { duration: cfg.opacityDuration }
            }
          }),
          (0, import_jsx_runtime14.jsx)("span", { ref: labelRef, className: "dialkit-slider-label", children: label }),
          editing ? mkInput() : mkValue()
        ]
      }
    ) });
  }
  if (variant === "ub-2" || variant === "ub-5") {
    const isDiscrete = variant === "ub-2";
    const ZONES = [{ label: "×10", mult: 10 }, { label: "×1", mult: 1 }, { label: "×0.1", mult: 0.1 }, { label: "×0.01", mult: 0.01 }];
    const [speedMult, setSpeedMult] = (0, import_react18.useState)(1);
    const onDown = (e) => {
      var _a3;
      if (editing) return;
      e.preventDefault();
      e.currentTarget.setPointerCapture(e.pointerId);
      const rect = (_a3 = rowRef.current) == null ? void 0 : _a3.getBoundingClientRect();
      dragState.current = { active: true, startX: 0, startVal: value, accDx: isDiscrete ? (rect == null ? void 0 : rect.top) ?? 0 : rect ? rect.top + rect.height / 2 : 0 };
      setDragging(true);
      setSpeedMult(1);
    };
    const onMove = (e) => {
      if (!dragState.current.active) return;
      let mult;
      if (isDiscrete) {
        const rowTop = dragState.current.accDx, rowH = 36, relY = e.clientY - rowTop, zone = 50;
        if (relY < -zone) mult = 10;
        else if (relY < rowH * 0.5) mult = 1;
        else if (relY < rowH + zone) mult = 0.1;
        else mult = 0.01;
      } else {
        const centerY = dragState.current.accDx;
        const dy = centerY - e.clientY;
        mult = Math.max(1e-3, Math.min(1e3, Math.pow(10, dy / 50)));
      }
      setSpeedMult(mult);
      dragState.current.startVal += e.movementX * step * mult;
      onChange(rnd(dragState.current.startVal));
    };
    const onUp = () => {
      dragState.current.active = false;
      setDragging(false);
      setSpeedMult(1);
    };
    const speedLabel = isDiscrete ? ((_b = ZONES.find((z) => z.mult === speedMult)) == null ? void 0 : _b.label) ?? "×1" : speedMult >= 1 ? `×${speedMult.toFixed(speedMult >= 10 ? 0 : 1)}` : `×${speedMult.toFixed(speedMult >= 0.1 ? 2 : 3)}`;
    return (0, import_jsx_runtime14.jsxs)("div", {
      ref: rowRef,
      className: `ub-zones ${isActive ? "ub-zones--active" : ""}`,
      onPointerDown: onDown,
      onPointerMove: onMove,
      onPointerUp: onUp,
      onPointerCancel: onUp,
      onMouseEnter: () => setHovered(true),
      onMouseLeave: () => setHovered(false),
      children: [
        dragging ? (0, import_jsx_runtime14.jsx)("div", { className: "ub-zones__badge", children: speedLabel }) : null,
        (0, import_jsx_runtime14.jsx)("span", { ref: labelRef, className: "dialkit-slider-label", children: label }),
        editing ? mkInput() : mkValue()
      ]
    });
  }
  if (variant === "ub-3" || variant === "ub-6") {
    const alwaysShowMarks = variant === "ub-6";
    const hasMomentum = variant === "ub-6";
    const [zoom, setZoom] = (0, import_react18.useState)(100);
    const velRef = (0, import_react18.useRef)(0);
    const frameRef = (0, import_react18.useRef)(0);
    const valRef = (0, import_react18.useRef)(value);
    valRef.current = value;
    const tapeRubber = useMotionValue(0);
    const tapeWidth = useTransform(tapeRubber, (s) => `calc(100% + ${Math.abs(s)}px)`);
    const tapeX = useTransform(tapeRubber, (s) => s < 0 ? s : 0);
    const niceStepFn = (range) => {
      const raw = range / 8;
      const mag = Math.pow(10, Math.floor(Math.log10(Math.abs(raw) || 1)));
      const r = raw / mag;
      return r <= 1.5 ? mag : r <= 3.5 ? 2 * mag : r <= 7.5 ? 5 * mag : 10 * mag;
    };
    const tickSz = niceStepFn(zoom);
    const halfR = zoom / 2;
    const wMin = value - halfR, wMax = value + halfR;
    const ticks = [];
    const tStart = Math.ceil(wMin / tickSz) * tickSz;
    for (let t = tStart; t <= wMax; t += tickSz) {
      const pct = (t - wMin) / zoom * 100;
      if (pct >= 0 && pct <= 100) ticks.push({ pct, major: Math.abs(t % (tickSz * 5)) < tickSz * 0.01 });
    }
    const onDown = (e) => {
      if (editing) return;
      e.preventDefault();
      e.target.setPointerCapture(e.pointerId);
      pointerDownPos.current = { x: e.clientX, y: e.clientY };
      isClickRef.current = true;
      setIsInteracting(true);
      cancelAnimationFrame(frameRef.current);
      if (wrapperRef.current) {
        wrapperRectRef.current = wrapperRef.current.getBoundingClientRect();
        scaleRef.current = wrapperRectRef.current.width / wrapperRef.current.offsetWidth;
      }
      dragState.current = { active: true, startX: e.clientX, startVal: value, accDx: zoom };
      velRef.current = 0;
    };
    const onMove = (e) => {
      var _a3;
      if (!isInteracting || !pointerDownPos.current) return;
      const dx = e.clientX - pointerDownPos.current.x;
      const distance = Math.abs(dx);
      if (isClickRef.current && distance > cfg.clickThreshold) {
        isClickRef.current = false;
        setDragging(true);
      }
      if (!isClickRef.current) {
        velRef.current = -e.movementX;
        const w = ((_a3 = wrapperRef.current) == null ? void 0 : _a3.offsetWidth) || 200;
        onChange(rnd(dragState.current.startVal - dx / w * dragState.current.accDx));
      }
    };
    const onUp = () => {
      var _a3;
      if (!isInteracting) return;
      dragState.current.active = false;
      setIsInteracting(false);
      setDragging(false);
      pointerDownPos.current = null;
      if (hasMomentum && !isClickRef.current) {
        let vel = velRef.current * (zoom / (((_a3 = wrapperRef.current) == null ? void 0 : _a3.offsetWidth) || 200));
        const coast = () => {
          vel *= 0.94;
          valRef.current = rnd(valRef.current + vel);
          onChange(valRef.current);
          if (Math.abs(vel) > step * 0.01) frameRef.current = requestAnimationFrame(coast);
        };
        if (Math.abs(vel) > step * 0.1) frameRef.current = requestAnimationFrame(coast);
      }
    };
    const onWheel = (e) => {
      if (editing) return;
      e.preventDefault();
      if (e.shiftKey) setZoom((z) => Math.max(0.1, Math.min(1e5, z * (e.deltaY > 0 ? 1.3 : 0.77))));
      else onChange(rnd(value + step * (e.deltaY > 0 ? 1 : -1)));
    };
    return (0, import_jsx_runtime14.jsx)("div", { ref: wrapperRef, className: "dialkit-slider-wrapper", children: (0, import_jsx_runtime14.jsxs)(
      motion.div,
      {
        className: `ub-tape ${alwaysShowMarks ? "ub-tape--always-marks" : ""} ${isActive ? "ub-tape--active" : ""}`,
        onPointerDown: onDown,
        onPointerMove: onMove,
        onPointerUp: onUp,
        onPointerCancel: onUp,
        onWheel,
        onMouseEnter: () => setHovered(true),
        onMouseLeave: () => setHovered(false),
        style: { width: tapeWidth, x: tapeX },
        children: [
          (0, import_jsx_runtime14.jsx)("div", { className: "dialkit-slider-hashmarks", children: ticks.map(
            (t, i) => (0, import_jsx_runtime14.jsx)("div", { className: `dialkit-slider-hashmark ${t.major ? "ub-tape__tick--major" : ""}`, style: { left: `${t.pct}%` } }, i)
          ) }),
          (0, import_jsx_runtime14.jsx)("div", { className: "ub-tape__hairline" }),
          (0, import_jsx_runtime14.jsx)("span", { ref: labelRef, className: "dialkit-slider-label", children: label }),
          editing ? mkInput() : mkValue()
        ]
      }
    ) });
  }
  return null;
}
var DEFAULT_LAYER_STACK_IDS = ["aurora", "image", "circle", "grain"];
var DEFAULT_LAYER_STACK_LABELS = {
  aurora: "Aurora Veil",
  image: "Image Plate",
  circle: "Particle Circle",
  grain: "Film Grain"
};
function parseLayerStackValue(value) {
  var parsed = [];
  var seen = /* @__PURE__ */ new Set();
  if (typeof value === "string") {
    var raw = value.split(/[,\s|>]+/).map((id2) => id2.trim().toLowerCase()).filter(Boolean);
    for (var i = 0; i < raw.length; i++) {
      var id = raw[i];
      if (!DEFAULT_LAYER_STACK_LABELS[id] || seen.has(id)) continue;
      seen.add(id);
      parsed.push(id);
    }
  }
  for (var j = 0; j < DEFAULT_LAYER_STACK_IDS.length; j++) {
    var fallback = DEFAULT_LAYER_STACK_IDS[j];
    if (!seen.has(fallback)) {
      seen.add(fallback);
      parsed.push(fallback);
    }
  }
  return parsed.slice(0, 4);
}
function reorderLayerIds(order, fromId, toId) {
  if (fromId === toId) return order;
  var fromIdx = order.indexOf(fromId);
  var toIdx = order.indexOf(toId);
  if (fromIdx === -1 || toIdx === -1) return order;
  var next = order.slice();
  next.splice(fromIdx, 1);
  next.splice(toIdx, 0, fromId);
  return next;
}
function UBLayerStackControl({ label, value, onChange }) {
  var [order, setOrder] = (0, import_react18.useState)(() => parseLayerStackValue(value));
  var [draggingId, setDraggingId] = (0, import_react18.useState)(null);
  var [overId, setOverId] = (0, import_react18.useState)(null);
  (0, import_react18.useEffect)(() => {
    setOrder(parseLayerStackValue(value));
  }, [value]);
  var commitOrder = (0, import_react18.useCallback)((next) => {
    setOrder(next);
    onChange(next.join(","));
  }, [onChange]);
  var onDragStart = (id, e) => {
    setDraggingId(id);
    setOverId(id);
    try {
      e.dataTransfer.setData("text/plain", id);
    } catch {
    }
    if (e.dataTransfer) {
      e.dataTransfer.effectAllowed = "move";
    }
  };
  var onDragOver = (id, e) => {
    if (!draggingId) return;
    e.preventDefault();
    if (id === draggingId) return;
    setOverId(id);
    setOrder((prev) => reorderLayerIds(prev, draggingId, id));
  };
  var onDrop = (id, e) => {
    e.preventDefault();
    if (!draggingId) return;
    var next = reorderLayerIds(order, draggingId, id);
    commitOrder(next);
    setDraggingId(null);
    setOverId(null);
  };
  var onDragEnd = () => {
    if (draggingId) {
      onChange(order.join(","));
    }
    setDraggingId(null);
    setOverId(null);
  };
  return (0, import_jsx_runtime14.jsxs)("div", { className: "ub-layer-stack-control", children: [
    (0, import_jsx_runtime14.jsx)("span", { className: "dialkit-labeled-control-label", children: label }),
    (0, import_jsx_runtime14.jsx)("div", { className: "ub-layer-stack", children: order.map((id, index) => (0, import_jsx_runtime14.jsxs)(
      "button",
      {
        type: "button",
        draggable: true,
        onPointerDown: (e) => e.stopPropagation(),
        onDragStart: (e) => onDragStart(id, e),
        onDragOver: (e) => onDragOver(id, e),
        onDrop: (e) => onDrop(id, e),
        onDragEnd,
        className: `ub-layer-stack__item ${draggingId === id ? "ub-layer-stack__item--dragging" : ""} ${overId === id && draggingId !== id ? "ub-layer-stack__item--over" : ""}`,
        children: [
          (0, import_jsx_runtime14.jsx)("span", { className: "ub-layer-stack__index", children: String(index + 1).padStart(2, "0") }),
          (0, import_jsx_runtime14.jsx)("span", { className: "ub-layer-stack__name", children: DEFAULT_LAYER_STACK_LABELS[id] ?? id }),
          (0, import_jsx_runtime14.jsx)("span", { className: "ub-layer-stack__grab", children: "|||" })
        ]
      },
      id
    )) })
  ] });
}
var TV_CLEANUP_MS = 580;
function ToggleVariant({ variant, label, value, onChange }) {
  var T = typeof window !== "undefined" && window.__UB_TUNING__ || {};
  var checked = !!value;
  var containerRef = (0, import_react20.useRef)(null);
  var pillRef = (0, import_react20.useRef)(null);
  var cleanupRef = (0, import_react20.useRef)(null);
  var mounted = (0, import_react20.useRef)(false);
  (0, import_react20.useLayoutEffect)(function() {
    var c = containerRef.current;
    if (!c || !pillRef.current) return;
    var btn = c.querySelector('button[data-active="true"]');
    if (!btn) return;
    pillRef.current.style.left = btn.offsetLeft + "px";
    pillRef.current.style.width = btn.offsetWidth + "px";
    mounted.current = true;
  }, [checked]);
  var handleToggle = (0, import_react20.useCallback)(function() {
    var next = !checked;
    var pill = pillRef.current;
    var c = containerRef.current;
    if (pill && c && mounted.current) {
      if (!(window.matchMedia && window.matchMedia("(prefers-reduced-motion: reduce)").matches)) {
        var btns = c.querySelectorAll("button");
        if (btns.length >= 2) {
          var startBtn = checked ? btns[1] : btns[0];
          var endBtn = checked ? btns[0] : btns[1];
          var sL = startBtn.offsetLeft;
          var sW = startBtn.offsetWidth;
          var eL = endBtn.offsetLeft;
          var eW = endBtn.offsetWidth;
          var goingRight = eL > sL;
          var bias = T.bias ?? 0.1;
          var midL, midR;
          if (goingRight) {
            midL = sL + sW * bias;
            midR = eL + eW * (1 - bias);
          } else {
            midL = eL + eW * bias;
            midR = sL + sW * (1 - bias);
          }
          var midW = midR - midL;
          pill.style.setProperty("--tv-sl", sL + "px");
          pill.style.setProperty("--tv-sw", sW + "px");
          pill.style.setProperty("--tv-ml", midL + "px");
          pill.style.setProperty("--tv-mw", midW + "px");
          pill.style.setProperty("--tv-el", eL + "px");
          pill.style.setProperty("--tv-ew", eW + "px");
          if (cleanupRef.current) clearTimeout(cleanupRef.current);
          pill.classList.remove("tv-liquid");
          void pill.offsetWidth;
          pill.classList.add("tv-liquid");
          cleanupRef.current = setTimeout(function() {
            pill.classList.remove("tv-liquid");
            cleanupRef.current = null;
          }, TV_CLEANUP_MS);
        }
      }
    }
    onChange(next);
  }, [checked, onChange]);
  var cls = variant.replace("ub-", "");
  return (0, import_jsx_runtime15.jsxs)("div", { className: "dialkit-labeled-control", children: [
    (0, import_jsx_runtime15.jsx)("span", { className: "dialkit-labeled-control-label", children: label }),
    (0, import_jsx_runtime15.jsxs)("div", {
      className: "tv-seg tv-" + cls,
      ref: containerRef,
      "data-checked": String(checked),
      children: [
        (0, import_jsx_runtime15.jsx)("div", { ref: pillRef, className: "tv-pill tv-" + cls + "-pill" }),
        (0, import_jsx_runtime15.jsx)("button", { className: "tv-btn tv-" + cls + "-btn", "data-active": String(!checked), onClick: handleToggle, children: "Off" }),
        (0, import_jsx_runtime15.jsx)("button", { className: "tv-btn tv-" + cls + "-btn", "data-active": String(checked), onClick: handleToggle, children: "On" })
      ]
    })
  ] });
}
function Panel({ panel, defaultOpen = true, inline = false }) {
  const [copied, setCopied] = (0, import_react3.useState)(false);
  const [isPanelOpen, setIsPanelOpen] = (0, import_react3.useState)(defaultOpen);
  const shortcutCtx = (0, import_react3.useContext)(ShortcutContext);
  const hasShortcuts = Object.keys(panel.shortcuts).length > 0;
  const values = (0, import_react3.useSyncExternalStore)(
    (cb) => DialStore.subscribe(panel.id, cb),
    () => DialStore.getValues(panel.id),
    () => DialStore.getValues(panel.id)
  );
  const presets = DialStore.getPresets(panel.id);
  const activePresetId = DialStore.getActivePresetId(panel.id);
  const handleAddPreset = () => {
    const nextNum = presets.length + 2;
    DialStore.savePreset(panel.id, `Version ${nextNum}`);
  };
  const handleCopy = () => {
    const jsonStr = JSON.stringify(values, null, 2);
    const instruction = `Update the useDialKit configuration for "${panel.name}" with these values:

\`\`\`json
${jsonStr}
\`\`\`

Apply these values as the new defaults in the useDialKit call.`;
    navigator.clipboard.writeText(instruction);
    setCopied(true);
    setTimeout(() => setCopied(false), 1500);
  };
  const renderControl = (control) => {
    var _a2;
    const value = values[control.path];
    switch (control.type) {
      case "slider":
        return (0, import_jsx_runtime16.jsx)(
          Slider,
          {
            label: control.label,
            value,
            onChange: (v) => DialStore.updateValue(panel.id, control.path, v),
            min: control.min,
            max: control.max,
            step: control.step,
            shortcut: control.shortcut,
            shortcutActive: shortcutCtx.activePanelId === panel.id && shortcutCtx.activePath === control.path
          },
          control.path
        );
      case "toggle":
        return (0, import_jsx_runtime16.jsx)(
          Toggle,
          {
            label: control.label,
            checked: value,
            onChange: (v) => DialStore.updateValue(panel.id, control.path, v),
            shortcut: control.shortcut,
            shortcutActive: shortcutCtx.activePanelId === panel.id && shortcutCtx.activePath === control.path
          },
          control.path
        );
      case "spring":
        return (0, import_jsx_runtime16.jsx)(
          SpringControl,
          {
            panelId: panel.id,
            path: control.path,
            label: control.label,
            spring: value,
            onChange: (v) => DialStore.updateValue(panel.id, control.path, v)
          },
          control.path
        );
      case "transition":
        return (0, import_jsx_runtime16.jsx)(
          TransitionControl,
          {
            panelId: panel.id,
            path: control.path,
            label: control.label,
            value,
            onChange: (v) => DialStore.updateValue(panel.id, control.path, v)
          },
          control.path
        );
      case "folder":
        return (0, import_jsx_runtime16.jsx)(Folder, { title: control.label, defaultOpen: control.defaultOpen ?? true, children: (_a2 = control.children) == null ? void 0 : _a2.map(renderControl) }, control.path);
      case "text":
        return (0, import_jsx_runtime16.jsx)(
          TextControl,
          {
            label: control.label,
            value,
            onChange: (v) => DialStore.updateValue(panel.id, control.path, v),
            placeholder: control.placeholder
          },
          control.path
        );
      case "select":
        return (0, import_jsx_runtime16.jsx)(
          SelectControl,
          {
            label: control.label,
            value,
            options: control.options ?? [],
            onChange: (v) => DialStore.updateValue(panel.id, control.path, v)
          },
          control.path
        );
      case "color":
        return (0, import_jsx_runtime16.jsx)(
          ColorControl,
          {
            label: control.label,
            value,
            onChange: (v) => DialStore.updateValue(panel.id, control.path, v)
          },
          control.path
        );
      case "action":
        return (0, import_jsx_runtime16.jsx)(
          "button",
          {
            className: "dialkit-button",
            onClick: () => DialStore.triggerAction(panel.id, control.path),
            children: control.label
          },
          control.path
        );
      case "ub-1":
      case "ub-2":
      case "ub-3":
      case "ub-4":
      case "ub-5":
      case "ub-6":
      case "ub-7":
      case "ub-8":
        return (0, import_jsx_runtime16.jsx)(UBControl, { variant: control.type, label: control.label, value, step: control.step ?? 1, onChange: (v) => DialStore.updateValue(panel.id, control.path, v) }, control.path);
      case "ub-layer-stack":
        return (0, import_jsx_runtime16.jsx)(UBLayerStackControl, { label: control.label, value, onChange: (v) => DialStore.updateValue(panel.id, control.path, v) }, control.path);
      case "ub-t1":
      case "ub-t6":
      case "ub-t11":
      case "ub-t12":
        return (0, import_jsx_runtime16.jsx)(ToggleVariant, { variant: control.type, label: control.label, value, onChange: (v) => DialStore.updateValue(panel.id, control.path, v) }, control.path);
      default:
        return null;
    }
  };
  const renderControls = () => {
    return panel.controls.map(renderControl);
  };
  const iconTransition = { type: "spring", visualDuration: 0.4, bounce: 0.1 };
  const toolbar = (0, import_jsx_runtime16.jsxs)(import_jsx_runtime16.Fragment, { children: [
    (0, import_jsx_runtime16.jsx)(
      motion.button,
      {
        className: "dialkit-toolbar-add",
        onClick: handleAddPreset,
        title: "Add preset",
        whileTap: { scale: 0.9 },
        transition: { type: "spring", visualDuration: 0.15, bounce: 0.3 },
        children: (0, import_jsx_runtime16.jsx)("svg", { viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2.5", strokeLinecap: "round", strokeLinejoin: "round", children: ICON_ADD_PRESET.map((d, i) => (0, import_jsx_runtime16.jsx)("path", { d }, i)) })
      }
    ),
    (0, import_jsx_runtime16.jsx)(
      PresetManager,
      {
        panelId: panel.id,
        presets,
        activePresetId,
        onAdd: handleAddPreset
      }
    ),
    (0, import_jsx_runtime16.jsx)(
      motion.button,
      {
        className: "dialkit-toolbar-add",
        onClick: handleCopy,
        title: "Copy parameters",
        whileTap: { scale: 0.9 },
        transition: { type: "spring", visualDuration: 0.15, bounce: 0.3 },
        children: (0, import_jsx_runtime16.jsx)("span", { style: { position: "relative", width: 16, height: 16 }, children: (0, import_jsx_runtime16.jsx)(AnimatePresence, { initial: false, mode: "wait", children: copied ? (0, import_jsx_runtime16.jsx)(
          motion.svg,
          {
            viewBox: "0 0 24 24",
            fill: "none",
            stroke: "currentColor",
            strokeWidth: "2",
            strokeLinecap: "round",
            strokeLinejoin: "round",
            style: { position: "absolute", inset: 0, width: 16, height: 16, color: "var(--dial-text-label)" },
            initial: { scale: 0.8, opacity: 0 },
            animate: { scale: 1, opacity: 1 },
            exit: { scale: 0.8, opacity: 0 },
            transition: { duration: 0.08 },
            children: (0, import_jsx_runtime16.jsx)("path", { d: ICON_CHECK })
          },
          "check"
        ) : (0, import_jsx_runtime16.jsxs)(
          motion.svg,
          {
            viewBox: "0 0 24 24",
            fill: "none",
            style: { position: "absolute", inset: 0, width: 16, height: 16, color: "var(--dial-text-label)" },
            initial: { scale: 0.8, opacity: 0 },
            animate: { scale: 1, opacity: 1 },
            exit: { scale: 0.8, opacity: 0 },
            transition: { duration: 0.08 },
            children: [
              (0, import_jsx_runtime16.jsx)("path", { d: ICON_CLIPBOARD.board, stroke: "currentColor", strokeWidth: "2", strokeLinejoin: "round" }),
              (0, import_jsx_runtime16.jsx)("path", { d: ICON_CLIPBOARD.sparkle, fill: "currentColor" }),
              (0, import_jsx_runtime16.jsx)("path", { d: ICON_CLIPBOARD.body, stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round" })
            ]
          },
          "clipboard"
        ) }) })
      }
    )
  ] });
  return (0, import_jsx_runtime16.jsx)("div", { className: "dialkit-panel-wrapper", children: (0, import_jsx_runtime16.jsx)(Folder, { title: panel.name, defaultOpen, isRoot: true, inline, onOpenChange: setIsPanelOpen, toolbar, children: renderControls() }) });
}
var _a;
var isDevDefault = typeof process !== "undefined" && "development" ? true : typeof import.meta !== "undefined" && ((_a = import.meta.env) == null ? void 0 : _a.MODE) ? import.meta.env.MODE !== "production" : true;
function DialRoot({ position = "top-right", defaultOpen = true, mode = "popover", theme = "system", productionEnabled = isDevDefault }) {
  if (!productionEnabled) return null;
  const [panels, setPanels] = (0, import_react2.useState)([]);
  const [mounted, setMounted] = (0, import_react2.useState)(false);
  const inline = mode === "inline";
  const panelRef = (0, import_react2.useRef)(null);
  const [dragOffset, setDragOffset] = (0, import_react2.useState)(null);
  const [activePosition, setActivePosition] = (0, import_react2.useState)(position);
  const lastDragOffset = (0, import_react2.useRef)(null);
  const draggingRef = (0, import_react2.useRef)(false);
  const dragStartRef = (0, import_react2.useRef)(null);
  const didDragRef = (0, import_react2.useRef)(false);
  (0, import_react2.useEffect)(() => {
    setMounted(true);
    setPanels(DialStore.getPanels());
    const unsubscribe = DialStore.subscribeGlobal(() => {
      setPanels(DialStore.getPanels());
    });
    return unsubscribe;
  }, []);
  (0, import_react2.useEffect)(() => {
    if (!panelRef.current || inline) return;
    const observer = new MutationObserver(() => {
      var _a2;
      const inner = (_a2 = panelRef.current) == null ? void 0 : _a2.querySelector(".dialkit-panel-inner");
      if (!inner) return;
      const collapsed = inner.getAttribute("data-collapsed") === "true";
      if (!collapsed) {
        if (dragOffset) {
          lastDragOffset.current = dragOffset;
          const bubbleCenterX = dragOffset.x + 21;
          const midX = window.innerWidth / 2;
          setActivePosition(bubbleCenterX < midX ? "top-left" : "top-right");
        } else {
          setActivePosition(position);
        }
        setDragOffset(null);
      } else if (lastDragOffset.current) {
        setDragOffset(lastDragOffset.current);
      }
    });
    observer.observe(panelRef.current, { subtree: true, attributes: true, attributeFilter: ["data-collapsed"] });
    return () => observer.disconnect();
  }, [inline, dragOffset, position]);
  const handlePointerDown = (0, import_react2.useCallback)((e) => {
    var _a2;
    const inner = (_a2 = panelRef.current) == null ? void 0 : _a2.querySelector(".dialkit-panel-inner");
    if (!inner || inner.getAttribute("data-collapsed") !== "true") return;
    const rect = panelRef.current.getBoundingClientRect();
    dragStartRef.current = {
      pointerX: e.clientX,
      pointerY: e.clientY,
      elX: rect.left,
      elY: rect.top
    };
    didDragRef.current = false;
    draggingRef.current = true;
    e.target.setPointerCapture(e.pointerId);
  }, []);
  const handlePointerMove = (0, import_react2.useCallback)((e) => {
    if (!draggingRef.current || !dragStartRef.current) return;
    const dx = e.clientX - dragStartRef.current.pointerX;
    const dy = e.clientY - dragStartRef.current.pointerY;
    if (!didDragRef.current && Math.abs(dx) + Math.abs(dy) < 4) return;
    didDragRef.current = true;
    setDragOffset({
      x: dragStartRef.current.elX + dx,
      y: dragStartRef.current.elY + dy
    });
  }, []);
  const handlePointerUp = (0, import_react2.useCallback)((e) => {
    var _a2;
    if (!draggingRef.current) return;
    draggingRef.current = false;
    dragStartRef.current = null;
    if (didDragRef.current) {
      e.stopPropagation();
      const inner = (_a2 = panelRef.current) == null ? void 0 : _a2.querySelector(".dialkit-panel-inner");
      if (inner) {
        const blocker = (ev) => {
          ev.stopPropagation();
        };
        inner.addEventListener("click", blocker, { capture: true, once: true });
      }
    }
  }, []);
  if (!mounted || typeof window === "undefined") {
    return null;
  }
  if (panels.length === 0) {
    return null;
  }
  const dragStyle = dragOffset ? {
    top: dragOffset.y,
    left: dragOffset.x,
    right: "auto",
    bottom: "auto"
  } : void 0;
  const content = (0, import_jsx_runtime17.jsx)(ShortcutListener, { children: (0, import_jsx_runtime17.jsx)("div", { className: "dialkit-root", "data-mode": mode, "data-theme": theme, children: (0, import_jsx_runtime17.jsx)(
    "div",
    {
      ref: panelRef,
      className: "dialkit-panel",
      "data-position": inline ? void 0 : dragOffset ? void 0 : activePosition,
      "data-mode": mode,
      style: dragStyle,
      onPointerDown: !inline ? handlePointerDown : void 0,
      onPointerMove: !inline ? handlePointerMove : void 0,
      onPointerUp: !inline ? handlePointerUp : void 0,
      children: panels.map((panel) => (0, import_jsx_runtime17.jsx)(Panel, { panel, defaultOpen: inline || defaultOpen, inline }, panel.id))
    }
  ) }) });
  if (inline) {
    return content;
  }
  return (0, import_react_dom.createPortal)(content, document.body);
}
function ButtonGroup({ buttons }) {
  return (0, import_jsx_runtime18.jsx)("div", { className: "dialkit-button-group", children: buttons.map((button, index) => (0, import_jsx_runtime18.jsx)(
    "button",
    {
      className: "dialkit-button",
      onClick: button.onClick,
      children: button.label
    },
    index
  )) });
}
function formatShortcutKey(sc) {
  if (!sc.key) return "—";
  const mod = sc.modifier === "alt" ? "⌥" : sc.modifier === "shift" ? "⇧" : sc.modifier === "meta" ? "⌘" : "";
  return `${mod}${sc.key.toUpperCase()}`;
}
function formatInteraction(sc) {
  const interaction = sc.interaction ?? "scroll";
  switch (interaction) {
    case "scroll":
      return sc.key ? "key+scroll" : "scroll";
    case "drag":
      return "key+drag";
    case "move":
      return "key+move";
    case "scroll-only":
      return "scroll";
  }
}
function ShortcutsMenu({ panelId }) {
  const [isOpen, setIsOpen] = (0, import_react21.useState)(false);
  const triggerRef = (0, import_react21.useRef)(null);
  const dropdownRef = (0, import_react21.useRef)(null);
  const [pos, setPos] = (0, import_react21.useState)({ top: 0, right: 0 });
  const open = (0, import_react21.useCallback)(() => {
    var _a2;
    const rect = (_a2 = triggerRef.current) == null ? void 0 : _a2.getBoundingClientRect();
    if (rect) {
      setPos({ top: rect.bottom + 4, right: window.innerWidth - rect.right });
    }
    setIsOpen(true);
  }, []);
  const close = (0, import_react21.useCallback)(() => setIsOpen(false), []);
  const toggle = (0, import_react21.useCallback)(() => {
    if (isOpen) close();
    else open();
  }, [isOpen, open, close]);
  (0, import_react21.useEffect)(() => {
    if (!isOpen) return;
    const handler = (e) => {
      var _a2, _b;
      const target = e.target;
      if (((_a2 = triggerRef.current) == null ? void 0 : _a2.contains(target)) || ((_b = dropdownRef.current) == null ? void 0 : _b.contains(target))) return;
      close();
    };
    document.addEventListener("mousedown", handler);
    return () => document.removeEventListener("mousedown", handler);
  }, [isOpen, close]);
  const panel = DialStore.getPanel(panelId);
  if (!panel) return null;
  const shortcuts = Object.entries(panel.shortcuts);
  if (shortcuts.length === 0) return null;
  const rows = shortcuts.map(([path, shortcut]) => {
    const findLabel = (controls) => {
      for (const c of controls) {
        if (c.path === path) return c.label;
        if (c.type === "folder" && c.children) {
          const found = findLabel(c.children);
          if (found) return found;
        }
      }
      return path;
    };
    return {
      path,
      shortcut,
      label: findLabel(panel.controls)
    };
  });
  return (0, import_jsx_runtime19.jsxs)(import_jsx_runtime19.Fragment, { children: [
    (0, import_jsx_runtime19.jsx)(
      motion.button,
      {
        ref: triggerRef,
        className: "dialkit-shortcuts-trigger",
        onClick: toggle,
        title: "Keyboard shortcuts",
        whileTap: { scale: 0.9 },
        transition: { type: "spring", visualDuration: 0.15, bounce: 0.3 },
        children: (0, import_jsx_runtime19.jsxs)("svg", { viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: [
          (0, import_jsx_runtime19.jsx)("rect", { x: "2", y: "6", width: "20", height: "12", rx: "2" }),
          (0, import_jsx_runtime19.jsx)("path", { d: "M6 10H6.01" }),
          (0, import_jsx_runtime19.jsx)("path", { d: "M10 10H10.01" }),
          (0, import_jsx_runtime19.jsx)("path", { d: "M14 10H14.01" }),
          (0, import_jsx_runtime19.jsx)("path", { d: "M18 10H18.01" }),
          (0, import_jsx_runtime19.jsx)("path", { d: "M8 14H16" })
        ] })
      }
    ),
    (0, import_react_dom4.createPortal)(
      (0, import_jsx_runtime19.jsx)(AnimatePresence, { children: isOpen && (0, import_jsx_runtime19.jsxs)(
        motion.div,
        {
          ref: dropdownRef,
          className: "dialkit-root dialkit-shortcuts-dropdown",
          style: { position: "fixed", top: pos.top, right: pos.right },
          initial: { opacity: 0, y: 4, scale: 0.97 },
          animate: { opacity: 1, y: 0, scale: 1 },
          exit: { opacity: 0, y: 4, scale: 0.97, pointerEvents: "none" },
          transition: { type: "spring", visualDuration: 0.15, bounce: 0 },
          children: [
            (0, import_jsx_runtime19.jsx)("div", { className: "dialkit-shortcuts-title", children: "Keyboard Shortcuts" }),
            (0, import_jsx_runtime19.jsx)("div", { className: "dialkit-shortcuts-list", children: rows.map((row) => (0, import_jsx_runtime19.jsxs)("div", { className: "dialkit-shortcuts-row", children: [
              (0, import_jsx_runtime19.jsx)("span", { className: "dialkit-shortcuts-row-key", children: formatShortcutKey(row.shortcut) }),
              (0, import_jsx_runtime19.jsx)("span", { className: "dialkit-shortcuts-row-label", children: row.label }),
              (0, import_jsx_runtime19.jsx)("span", { className: "dialkit-shortcuts-row-mode", children: formatInteraction(row.shortcut) })
            ] }, row.path)) }),
            (0, import_jsx_runtime19.jsx)("div", { className: "dialkit-shortcuts-hint", children: "See pill badges on controls for keys" })
          ]
        }
      ) }),
      document.body
    )
  ] });
}
export {
  ButtonGroup,
  ColorControl,
  DialRoot,
  DialStore,
  EasingVisualization,
  Folder,
  PresetManager,
  SelectControl,
  ShortcutsMenu,
  Slider,
  SpringControl,
  SpringVisualization,
  TextControl,
  Toggle,
  TransitionControl,
  useDialKit
};
//# sourceMappingURL=dialkit.js.map
